# BioImageFlow Agent Entry Point

Follow `AGENTS.md` for repository instructions. The guidance is model-agnostic: use the same backend authority, validation, workflow-local tool, and saved-workflow safety rules regardless of agent runtime.

Primary resources:

- `.agents/skills/bioimageflow-platform/SKILL.md`
- `.agents/resources/rest-cookbook.md`
- `.agents/resources/frontend-state-map.md`
