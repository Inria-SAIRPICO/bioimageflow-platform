# Review

## Tools panel

No need for the number of tool in each category.
The category should have a slightly darker background so we differenciate tools from categories.

### Manage tools dialog


## Canvas


## Nodes panel

The description should always be shown, and the "Help toggle button" is useless for now.


The two icons of the None toggle button should be inverted (Ø / Pencil). Maybe the button should only show when the parameter can be null ; but current the parameters must always be defined? I think the type check system do not allow null params, right? Yet, some CLI tool like Atlas have default values, and it is sometimes important not to provide an argument to let the tool use the default one, or have the default behavior.

## Others
