# BioImageFlow Opener

This extension is packaged with BioImageFlow's embedded code-server environment.
It exposes a loopback-only `/open` endpoint that focuses files without changing the current code-server workspace.

Build from this directory with `npm run package`.
