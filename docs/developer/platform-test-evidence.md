---
orphan: true
---

# Platform test campaign evidence

This archive preserves the completed validation and release history from the checkpoint at `eb0dc74`.
The heading dates and revision identities below describe historical runs; they do not establish current certification without checking whether relevant code has changed.
Use [Campaign progress](platform-test-progress.md) for current status and [Campaign plan](platform-test-plan.md) for priorities and completion requirements.
Append concise per-task evidence here after integration; keep raw logs and traces in ignored artifacts.
Record the exact command, source revision/diff, result, duration, exclusions/skips/retries, package versions where relevant, and remaining limitations.

## Campaign strategy revision — 2026-09-14

At `eb0dc74` plus the campaign-documentation diff, `scripts/test check docs` passed in 5s with no warnings or skipped phases.
The revision sets Sol/high orchestration, main-GUI priority waves, systematic all-feature coverage requirements, concise checkpoints, and the current single-click information/double-click creation contract.
No product code or tests changed; application, browser, and external certification were not rerun for this documentation-only task.
The task commit is discoverable with `git log -1 -- docs/developer/platform-test-plan.md`.

## Completed feature validation

- Wave 6 workflow grouping: integrated commits `a36a4cb`, `da0ded6`, and `152c82b` add an exact incoming positional-DataFrame utility regression and a real multi-selection canvas journey for mixed DataFrame/field boundaries. The browser case proves exact embedded nodes/internal edge, stable published input/output identities and targets, four external rewrites, accepted validation, atomic Undo/Redo, Save, reload, and rendered topology. The focused utility selector passed 3/3 in 2s; the exact journey passed Chromium 1/1 in 9s after two fixture-only authoring failures and Firefox 1/1 in 11s. `scripts/test check frontend` passed lint, type checking, build, and 1,298 tests in 20s on `e181f44`-equivalent content; `scripts/test check browser` passed 84/84 Chromium tests in 222s; and `scripts/test check cross-browser-smoke` passed 14 Chromium and 14 Firefox journeys in 66s on patch-equivalent final content before the critical-tag commit. Backend, full, and external lanes were omitted because platform behavior and APIs did not change. Invalid/refused grouping, execution failure/recovery, and recursive clipboard transport remain gaps.
- Wave 6 Save As and deletion lifecycle: integrated commits `a25ac17` and `0b3ee1e` fix destination graph identity and prove an independent copied graph, absent copied results, exact source retention, copy-only GUI edit/save, switching, reload, deletion Cancel, captured deletion generation, sibling isolation, and durable empty-last-workflow state. Before the repair, the exact Chromium Save As selector failed in 33s with the destination showing the source graph identity. The final focused MenuBar selector passed 1 test with 68 deselected in 2s; exact Save As passed Chromium 1/1 in 13s and Firefox 1/1 in 15s; the three exact Chromium deletion selectors passed 3/3 in 13s. `scripts/test check frontend` passed lint, type checking, build, and 1,297 tests in 19s, and `scripts/test check browser` passed all 83 Chromium journeys in 222s. Two pre-final draft-independence attempts timed out in 52s and 67s because of unreliable canvas dragging; the permanent journey uses a stable real GUI rename. Completion lanes had no failures, retries, flakes, or skips; Firefox is not part of the complete Chromium browser lane. Invalid destination/stale-generation refusal and transport/server recovery remain gaps. At source revision `0b3ee1e` plus the reconciliation diff, the dashboard verifier reported 98 rows, 457 applicable/172 verified and 220 primary/150 verified, and `scripts/test check docs` passed in 2s.
- Main GUI workflow journey: commit `4208769` uses actual controls to create and open workflows, inspect exact SeedNumbers documentation by single-click without mutation, create nodes by double-click and drag, pointer-connect compatible DataFrames, edit and persist `IncrementNumbers.number`, run Direct computation, inspect exact visible `1/one/2`, `2/two/3`, and `3/three/4` rows, Save, switch workflows, and reopen the identical accepted graph. The exact journey passed Chromium in 33s and Firefox in 42s on the task branch; `scripts/test check frontend` passed lint, type checking, build, and 1,273 unit tests in 42s; `scripts/test check docs` passed in 51s after an initial sandbox DNS block. A Chromium browser completion passed this journey and its preceding 49 cases before an unrelated Save As failure with eight fail-fast deselections; the exact Save As case then passed in 26s and the entire affected `workflow-crud` plus `workflow-interface` tail passed 11/11 in 58s. After integration, the exact journey passed together in Chromium and Firefox, 2/2 in 43s at `4208769`; an initial sandbox attempt could not allocate local ports and collected no tests. No worker, remote, distributed, or full-Firefox-lane claim is made.
- ISSUE-005 selected execution admission: GPT-6 Astra/high reproduced the manager failure read-only at `b6072f5` with installed BioImageFlow 0.7.1 in 6.02s and returned `safe-to-fix`; public selected-node computation returned exact rows from the intact full graph despite two unrelated `missing_tool` diagnostics. Sol/medium implemented `38e14f2`; orchestrator review then reproduced dangling-source and dangling-target `KeyError` failures, and `8908388` added the total traversal guard and two regressions. At integrated `8908388`, `scripts/test focus backend tests/test_services/test_execution_manager.py -k selected_run` passed 15 selected with 57 deselected in 4s; the exact real compiler regression passed in 5s; the exact GUI journey passed Chromium and Firefox, 2/2 in 41s. `scripts/test check app` passed in 168s with backend lint, 1,606 deterministic backend tests and 9 deselected, 1,273 frontend units, frontend lint/type/build, 7 logging-order tests, and 11 critical Chromium journeys. External certification and non-critical browser tests were intentionally omitted by the app lane; no retries or skips occurred in the focused integrated runs.
- Everyday node-state editing: commit `5412406` adds visible bulk enable/disable controls and duplicate-name feedback, and routes header double-click collapse through the Vue Flow canvas boundary; `ec0ca3b` corrects the fixture to the in-scope Direct/sequential policy. The three journeys prove selection, Shift multiselection, empty-click deselection, bulk and single-node enablement, valid Unicode rename with stable graph identity, duplicate refusal without a draft revision, collapse, and accepted-draft reload. Exact branch runs passed Chromium 3/3 in 28s and Firefox 3/3 in 47s; `scripts/test check frontend` passed lint, type checking, build, and 1,273 units in 39s. Browser completion passed the three new cases and 35 others before the excluded managed-remote journey failed with 25 fail-fast deselections in 189s; campaign collection still selected all three and reported 59 selected/2 excluded per browser in 5s. After the sequential correction, Chromium passed 3/3 in 27s; after integration, Chromium and Firefox passed 6/6 in 56s. No retries or skips occurred in exact runs; bulk-action undo atomicity remains unverified.
- Connection and history editing: commits `c186a42`, `99599f9`, `cb1d2b4`, `daa2f8a`, `72d80c1`, and `e0aa123` make bulk enablement one history transition, correct the all-node fallback, ignore no-op parameter emissions, expose per-canvas Undo/Redo availability, and add pointer reconnection, mixed edit history, and workflow-switch isolation journeys. Orchestrator review caught the short-circuit fallback before integration and rejected two arbitrary browser sleeps; follow-ups added exact all-ID unit coverage and observable disabled Undo state. At the final task revision, the six cases passed Chromium 6/6 in 49s and Firefox 6/6 in 76s; 1,278 frontend tests plus lint/type/build passed in 38s; cross-browser smoke passed 11 Chromium and 11 Firefox cases in 80s; focused command and parameter units passed 38/38 in 7s and 23/23 in 9s. After integration, all 12 Chromium/Firefox cases passed in 112s and `scripts/test check frontend` passed lint, type checking, build, and all 1,280 units in 36s. No skips, deselections, retries, or external blockers occurred in those successful runs.
- Clipboard and active-root shortcuts: task commits `7ca6934` and `aecce68`, integrated as `3bb5a10` and `790345a`, add actual clipboard and keyboard GUI journeys plus shared text-entry focus detection. The clipboard journey verifies fresh pasted node/edge identities, preservation of only the selected internal edge, accepted-draft reload, and malformed-payload refusal with exact zero mutation. The shortcut journey proves active-workflow isolation for Delete/Backspace, select-all, copy/paste, Save, forced validation, fit, Escape, and search focus, while a focused rename field retains focus and causes no accepted mutation. The repair fixes root Ctrl/Cmd+S being swallowed without saving, absent Escape and Ctrl/Cmd+F behavior, unsafe global Save routing from text fields, and modified Enter committing a rename. At task revision `aecce68`, the focused keyboard unit passed 1/1 in 7s; each journey passed Chromium in 24s and 27s and Firefox in 29s and 29s; `scripts/test check frontend` passed lint, type checking, build, and 1,281 tests in 38s; `scripts/test check cross-browser-smoke` passed 11 Chromium and 11 Firefox cases in 79s. After integration at `790345a`, `scripts/test focus e2e tests/e2e/clipboard-shortcuts.spec.ts --project=chromium --project=firefox` passed 4/4 in 49s; an initial sandbox attempt could not allocate ports and collected no tests. Successful final runs used no skips, deselections, or retries; browser-all/full were outside this localized completion scope, and malformed native clipboard permission remains a controlled boundary because Firefox Playwright cannot grant clipboard-read.
- Typed parameter controls: task commit `89fc0ab`, integrated as `1261326`, adds a deterministic real ProcessingTool fixture and an actual GUI journey for Literal enum, boolean, invalid JSON-list refusal without revision advance, valid recovery, default reset, pin reveal, column connection, connected-field read-only presentation, accepted validation and graph state, exact workflow identity, Save, switch, and identical reopen. The batch fixes Node Panel comparing raw parameter keys with encoded connected-input handles, which left accepted connected inputs editable; the focused unit now verifies the canonical handle and source label. At task revision `89fc0ab`, the exact unit passed 1/1 with 6 skipped in 8s; the journey passed Chromium and Firefox separately in 32s each; `scripts/test check frontend` passed 1,281 tests plus lint/type/build in 37s; `scripts/test check backend` passed 1,606 tests with 9 external deselections plus 7 logging-order tests in 101s. Cross-browser smoke retained Chromium 11/11 after Firefox encountered an unrelated managed-execution path-input timing failure; the exact failed Firefox case passed in 33s and the complete Firefox critical rerun passed 11/11 in 75s. After integration at `1261326`, `scripts/test focus e2e tests/e2e/parameter-controls.spec.ts --project=chromium --project=firefox` passed 2/2 in 47s. Successful final runs used no skips or retries beyond the six focused-unit deselections; full, browser-all, and external certification were outside this scoped completion.
- Nested private editing: commit `af58e4e` adds a primary GUI journey over a statelessly validated Direct/sequential fixture with stable DataFrame/field port IDs, a parent DataFrame edge, and a constant binding. It proves durable private snapshot isolation before Apply, compatible Apply with preserved parent edge/binding, root Save plus reload/reopen, and confirmation-based Discard of a later durable private edit. On the task branch the exact journey passed Chromium in 26s and Firefox in 31s; `scripts/test check frontend` passed 1,273 units plus lint/type/build in 40s; `scripts/test check cross-browser-smoke` passed 11 Chromium and 11 Firefox cases in 79s. After integration, the exact journey passed Chromium and Firefox, 2/2 in 38s. Initial dependency and sandbox-port attempts were blocked before collection; no successful run used retries or skips. Stale-parent conflict, incompatible/removal confirmation, execution, crash recovery, and recursive source ownership remain explicit gaps.
- ISSUE-006 specialist assessment: GPT-6 Astra/high inspected the uncommitted nested stale-parent overwrite proposal read-only at worktree base `299eebe` against main `a4745fc` and returned `not-a-defect`. V2 §7 requires refusal and no parent mutation, which the baseline already provides; it does not require overwrite/reload buttons. The proposal was rejected because its content-only child comparison omitted generation and surrounding connections, allowed newer private edits during an unlocked await to be marked clean, did not safely cover lock/unmount/second-change state, and assumed synchronous application before durable parent acceptance. No files or tests were changed by the review. The required follow-up evidence is the existing refusal plus durable private state and confirmed Discard/reopen; destructive connected-port confirmation remains an independent explicit contract.
- ISSUE-006 stale-parent evidence: commits `b149daf` and `f920c9b` add the test-only journey required by the `not-a-defect` disposition. The exact integrated selector passed Chromium and Firefox, 2/2 in 38s, and proves exact pre/post parent graph and revision equality, durable dirty private state, visible refusal, canceled close, confirmed Discard, and latest-parent reopen. The unsafe overwrite-control proposal was fully discarded. A separate task-branch frontend completion covering this selector passed 1,273 units plus lint/type/build in 34s, and cross-browser smoke passed 11 Chromium plus 11 Firefox cases in 91s; those broader results also contained an unintegrated destructive-port implementation and are not evidence for that implementation's safety.
- Destructive nested-interface application: task commits `b883f89`, `969516a`, and `2bda18e`, integrated as `b2e1152`, `f59419b`, and `cf794fb`, implement explicit confirmation and session-scoped parent application with exact accepted private graph/revision, parent before/applied graphs, effects, session identity, both canvas registrations, and lock checks. The journey proves Cancel causes zero parent PUTs; confirmed application atomically removes a parent edge, binding, forwarded output, and only the affected target of a multi-target forwarded input. It forces parent persistence failure, preserves a later unrelated sibling GUI edit during safe retry, preserves newer private B and monotonic revision while acknowledging A, applies B, saves, and reloads the exact final graph. A separate delayed-A/remount/coalesced-Save journey proves dirty-title projection, discard protection, retained retry, and final clean state. Initial Astra review rejected `b883f89` as unsafe because delayed A could overwrite B, enclosing interface references survived, retry trapped after failure, lifecycle guards were absent, and schema equality depended on key order. A second review found unrelated-parent retry and remount presentation blockers in `969516a`; after `2bda18e`, the final read-only Astra delta review returned `safe-as-is`. At task revision `2bda18e`, focused units passed 9/9 in 6s; the sibling-recovery and remount cases passed Chromium separately in 27s and 24s and Firefox together 2/2 in 43s; `scripts/test check frontend` passed 1,282 tests plus lint/type/build in 35s; cross-browser smoke passed 11 Chromium and 11 Firefox cases in 82s. At integrated `cf794fb`, the stale-parent, destructive-apply, and remount journeys passed Chromium and Firefox 6/6 in 54s, and `scripts/test check frontend` passed 1,291 tests plus lint/type/build in 34s. Successful final runs had no skips, deselections, or retries; intermediate failures were test mechanics or the two specialist-identified safety gaps and are not counted as passing evidence.
- Real nested Direct execution: task commit `17dd309`, integrated as `fd96f8f`, adds a deterministic declared downstream transform and the GUI journey `nested-execution.spec.ts`. The exact accepted Direct/sequential graph runs `SeedNumbers` through stable child DataFrame input `stable-table-input`, scoped `nested_step/nested_increment`, stable child output `stable-increment-output`, and root `IncrementAgainNumbers`. The actual Node Data tables show nested `number_plus_one` rows `2,3,4` and downstream `(number_plus_one, number_plus_two)` rows `(2,3),(3,4),(4,5)`; the execution snapshot attributes status to the exact root/revision and scoped child ID. GUI Save, reload, sibling switch, exact-root reopen, and repeated result inspection preserve the graph and results while the sibling graph/hash/generation remain exact and its result stays 404. At task revision `17dd309`, Chromium passed 1/1 in 23s, Firefox passed 1/1 in 32s, the focused backend seed test passed 1/1 in 8s, `scripts/test check frontend` passed 1,291 tests plus lint/type/build in 35s, and `scripts/test check app` passed in 194s with 1,606 backend tests and 9 external deselections, 1,291 frontend tests, 7 logging-order tests, and 11 critical Chromium journeys. At integrated `fd96f8f`, the exact Chromium/Firefox journey passed 2/2 in 36s. Successful final runs had no skips or retries; the app lane intentionally omitted external certification and non-critical browser tests. Initial sandbox/local-port and fixture-oracle failures occurred before the final valid fixture and are not passing evidence.
- Bulk node deletion and accepted output clearing: task commits `d9eff11` and `2a8c5cf`, integrated as `5aaac41` and `30faeb6`, add root Node Panel bulk Delete/Clear plus single Clear controls, captured active-canvas command routing, atomic node/incident-edge deletion, and selected latest-output projection removal. Real Shift-drag box selection drives the deletion journey; Cancel preserves the exact revision/graph, confirmation advances one revision, Undo/Redo each advance once, reload keeps the valid empty graph, and an inactive workflow remains exact. The Clear journey performs real execution, proves Cancel retains output with exact graph/revision, confirms selected output becomes 404 across reload without graph mutation, and hides unsupported Clear controls on nested canvases. The follow-up requires a non-null accepted draft and sends its exact graph; a forced draft PUT failure produces zero `/execution/clear` requests and retains output. This fixes the backend leaving `views/latest` pointers and `outputs/latest` materializations queryable after cache invalidation. At final task revision `2a8c5cf`, focused backend clearing passed 12 selected with 34 deselected in 5s; affected frontend units passed 72/72 in 4s; exact deletion and accepted Clear passed Chromium in 27s and 33s, both passed Firefox 2/2 in 44s, and the failed-persistence follow-up passed Chromium in 26s and Firefox in 28s. `scripts/test check frontend` passed 1,286 tests plus lint/type/build; `scripts/test check backend` passed 1,607 tests with 9 external deselections plus 7 logging-order tests; final `scripts/test check app` passed all deterministic backend/frontend/logging phases in 178s, while its Chromium phase was environment-blocked before collection by sandbox port allocation and the invalidated `scripts/test check browser-smoke` phase then passed 11/11 in 40s with server access. A full Chromium lane passed 44 tests including both new journeys before one campaign-excluded managed-remote assertion failed at test 45; that exact managed selector then passed 1/1 in 32s. At integrated `30faeb6`, the three affected cases passed Chromium and Firefox 6/6 in 40s. Successful final focused runs had no skips or retries; external certification and non-critical Firefox coverage remain outside this scoped completion.
- Recursive agent-copy source ownership: task commits `7a086a7`, `2988342`, `082d921`, and `9c3beb9`, integrated as `13a384b`, `9561b25`, `c911eb3`, and `e817d77`, extend the lower-level agent duplicate contract and add the visible “Save agent version as copy” GUI journey. A controlled real custom DataFrameTool is exported/imported into same-class, different-content root and embedded owned sources; the test verifies exact recursive graph identity, sorted owned-source IDs, four distinct paths and exact bytes, copy-only child editing from `+20` to `+40`, root GUI rows `2,3,4`, scoped child GUI rows `41,42,43`, no original result, and exact original saved document/draft preservation after switching, reload, and execution. Existing keep-canvas false/true GUI conflict variants remain intact. GPT-6 Astra/high first required restoration of the dropped variants plus stronger root-resolution and post-run immutability oracles, then returned `safe-as-is` at `9c3beb9`; no product defect was demonstrated. At that revision the focused backend selector passed both parameter cases in 3s, the three affected cases passed Chromium 3/3 in 17s and Firefox 3/3 in 27s, cross-browser smoke passed 11 Chromium and 11 Firefox cases in 54s, and E2E lint/diff checks passed. An earlier `scripts/test check app` at `2988342` passed in 164s with 1,606 backend tests and 9 external deselections, 1,291 frontend tests plus lint/type/build, 7 logging-order tests, and 11 critical Chromium journeys; later changes were test-only and their affected selectors/lint passed. At integrated `e817d77`, the same three cases passed Chromium 3/3 in 19s and Firefox 3/3 in 24s. Native editor, actual coding-agent/MCP authoring, packaged desktop behavior, external certification, and non-critical broad browser execution remain outside this bounded batch.
- Accepted root-draft recovery and lossless Discard: task commit `5060d47`, integrated as `dd20821`, strengthens the existing critical root Discard journey over a valid SeedNumbers→IncrementNumbers graph. An actual GUI parameter edit reaches an accepted valid backend draft; after removing the IndexedDB fallback, reload and sibling switching preserve its exact workflow identity, graph, edge/binding, revision, parameter, and dirty state. Close Cancel performs no mutation; a real agent CAS race makes stale Discard visibly fail while retaining the local canvas and remote draft; Keep my canvas rebases; confirmed Discard atomically increments the revision, restores the exact saved graph, closes, and reopens cleanly without stale values, sibling mutation, or an implicit workflow POST. No product defect or ambiguity was found. At `5060d47`, the exact journey passed Chromium 1/1 in 18s and Firefox 1/1 in 21s; focused startup ordering passed 2 with 4 skipped in 1s; focused discard coordination passed 3 with 22 skipped in 2s; `scripts/test check frontend` passed lint/type/build and 1,295 tests in 26s; cross-browser smoke passed 12 Chromium and 12 Firefox cases in 63s. At integrated `dd20821`, the exact journey passed Chromium 1/1 in 15s and Firefox 1/1 in 19s. A preliminary Chromium attempt failed because the test helper read the wrong parameter key and was corrected; no stress retry was used. No backend, full, or browser-all lane was required for this test-only localized batch; packaged crash/restart, native desktop dialogs, and crash during discard cleanup remain unautomated boundaries.
- Exact result-table projection and CSV export: task commits `9f596da`, `c8e9986`, and `3f58b37`, integrated as `ec0e32f`, `52006ae`, and `d1072ce`, add a dev-only deterministic 60-row DataFrame source and a primary real GUI result journey. The visible merged Node Data table filters `label` by `starts with keep-`, sorts `score` descending, proves the backend projection's exact per-node `source_rows` against every correlated visible source-row/label/score value, selects 25- and 50-row pages, navigates by Next and direct page input with exact requests/ranges, and parses the complete downloaded CSV header and all 30 filtered/sorted rows. No product defect or behavior/specification change was required. At `c8e9986`, the exact journey passed Chromium 1/1 in 24s and Firefox 1/1 in 24s; 16 focused frontend table/store cases passed in 4s; 15 focused projection/router backend cases passed with 31 deselected in 8s; the dev fixture router passed 3 cases in 52s including first isolated environment startup; and `scripts/test check app` passed in 165s with 1,607 deterministic backend tests and 9 deselected, 1,295 frontend tests plus lint/type/build, 7 logging-order tests, and 13 critical Chromium journeys. After the direct-page oracle follow-up at `3f58b37`, the exact case passed Chromium in 23s and Firefox in 28s. At integrated `d1072ce`, it passed Chromium in 22s and Firefox in 22s. Two preliminary Chromium failures corrected fixture assumptions about runtime engine selection and ordinary merged-table rendering; no flake or product failure was hidden. The app lane intentionally omitted external certification and non-critical browser tests; image actions, multi-selection projections, and column-width persistence remain separate gaps.
- ISSUE-007 active cooperative-worker cancellation: BioImageFlow commits `bdfb777`, `1cf95f4`, and `cf2efd4` propagate cancellation into active row/batch Wetlands handles, register each successful submission before the next, drain every submitted possible writer through partial submission and interrupted waits, and gate real held-writer regressions. Focused upstream validation passed 23 unit/race/timeout cases in 1.70s, 2 real held-writer row/batch cases in 5.27s, 1,378 fast units with 9 deselected in 111.08s, 527 fast integrations with 47 deselected in 78.64s, Ruff, Pyright, Sphinx, size, and import-boundary gates. Release commit `659acda` passed [normal CI](https://github.com/Inria-SAIRPICO/bioimageflow/actions/runs/34863426923) across 12 jobs and [Complete Wetlands validation](https://github.com/Inria-SAIRPICO/bioimageflow/actions/runs/34863450754) in 4m37s; tagged publication [succeeded](https://github.com/Inria-SAIRPICO/bioimageflow/actions/runs/34864151902), and `scripts/package_status.py` confirmed BioImageFlow 0.7.2 on PyPI. The explicit release selected only `bioimageflow`; the unpublished phasor package and locally newer segmentation and tracking package states were not part of this release.
- Platform cancellation adoption: commits `982c020`, `378b6c5`, `e40a661`, and `da2ced2` add the deterministic held worker, its lower-level contract, the exact GUI journey, importable worker annotations, and the published 0.7.2 pin. The journey verifies exact workflow/revision/run attribution, a completed upstream source, an active worker PID distinct from the backend, reload/WebSocket recovery, disabled Save/Clear without draft mutation, HTTP 200 Stop, cooperative acknowledgement before idle, retained exact upstream rows, absent partial output, and successful rerun values `4,8,12` from one non-backend worker PID. It passed Chromium 1/1 in 27s and Firefox 1/1 in 24s. `scripts/test check app` passed in 111s with backend lint and 1,609 tests plus 9 deselected, frontend lint/type/build and 1,295 tests, 7 logging-order tests, and 13 critical Chromium journeys. External certification and non-critical browser tests were intentionally omitted by the app lane; a separate cross-browser smoke was not stacked because the new non-critical journey passed explicitly in both browsers. The initial Chromium sandbox attempt could not allocate ports and collected no tests. Validation ran at pre-final-rebase `b3e8a2e`; the final rebase incorporated only campaign documentation, and an explicit application-tree diff was empty before integration at `da2ced2`.
- Real multi-selection results and table widths: task commit `c050207` GUI-creates and Direct-executes SeedNumbers→IncrementNumbers plus an independent deterministic result source. The exact journey proves compatible Shift-selection request order/roles, merged source-node/source-column metadata and source rows `0..2`, correlated visible rows, source navigation, independent stacked fallback with exact rows, and a real 50px merged-column resize that persists across selection navigation before Reset removes the scoped localStorage override. It passed Chromium 1/1 in 9s and Firefox 1/1 in 11s; 18 focused frontend table/width/store/source cases passed in 2s, 14 backend projection/router cases passed in 3s, and `scripts/test check frontend` passed 1,295 tests plus lint/type/build in 18s. No successful focused run used skips or retries. Preliminary authoring failures corrected obscured drop/navigation coordinates, one missing required parameter, and a selected-node toggle assumption without weakening data assertions.
- Subpixel canvas-drop assertion: separate commit `d8bd740` replaces exact integer equality with independent `<1px` X/Y bounds while retaining exact graph validation, node count, and tool identity. The old assertion twice failed in full Chromium at requested Y `180` versus persisted `179.21875`; its exact selector initially passed alone in 7s, then the bounded final assertion passed 1/1 in 8s and again as case 9 of the completion attempt. The final `scripts/test check browser` accounted 75/81 passing in 182s, including the new result journey and both existing width cases, before the unchanged nested child Save case left `Child *`; all five fail-fast tail cases passed separately 5/5 in 15s, so 80/81 were green but the browser lane is not claimed as passed. The nested Save case reproduced exactly 1/1 in 11s; accepted-snapshot and parent-PUT barrier experiments did not resolve it and were fully reverted before integration. Astra/high is assessing that independent blocker.
- ISSUE-008 nested Save canonicalization: commit `3422e1c` adds the backend-canonical `default: null` to both DataFrame input constructors and mounts the real nested editor in unit regressions for positional publication and child-port forwarding. Before the repair, the unchanged exact Chromium selector `workflow-interface.spec.ts:352` reproduced the accepted-write-but-dirty `Child *` state 1/1 in 12s. After the repair it passed Chromium 1/1 in 8s, four related nested Chromium journeys passed 4/4 in 15s, and the exact Firefox journey passed 1/1 in 9s. The focused canonical-default unit selector passed 2 with 23 deselected in 2s. `scripts/test check frontend` passed lint, typecheck, build, and 1,297 tests in 22s with no skips or deselections. `scripts/test check browser` passed all 81 Chromium journeys in 208s with no failures, skips, or deselections. The initial independent Astra reproduction was blocked before collection by sandbox port allocation, then reproduced normally; no retry or weakened browser assertion is part of the passing evidence.
- ISSUE-009 real failure diagnostics and correction/rerun: commit `4676ffb` suppresses only an empty-detail preliminary failed-progress ERROR so `_on_run_done` publishes the single enriched node-attributed traceback, while retaining immediate failed node state. The exact real sequential worker journey passed Chromium 1/1 in 25s and Firefox 1/1 in 26s; four focused fixture/manager cases passed in 2s, and the existing cancellation Chromium journey passed 1/1 in 26s. `scripts/test check app` passed in 120s at the patch-equivalent pre-rebase commit `aa78097`: backend lint and 1,610 deterministic tests with 9 external deselections, frontend lint/typecheck/build and 1,295 tests, 7 logging-order tests, and 13 critical Chromium journeys. The lane intentionally omitted external certification and non-critical browser tests. The task rebased twice without conflict over campaign documentation and an unrelated napari specification; range-diff proved exact patch equivalence before integration. Development attempts exposed and corrected PrimeVue blur, projected latest-path, stable locator, and fixture template assumptions; one run was externally interrupted by transient `pixi-install` failure before the worker started, and no flaky retry of a passing result was used.
- Real image/file viewing and export bytes: commits `624d986`, `8c3b19d`, `4a5015d`, and `7ab16e6` add a typed deterministic image/Path result fixture, drive the actual result-table Avivator action, and parse real GUI-downloaded ZIP bytes after a full run and selected rerun. The image journey passed Chromium 1/1 in 14s and Firefox in 9.2s; it proves exact displayed paths, readable preview, workflow-scoped TIFF/OME/offset bytes, and loaded Avivator iframe URLs. The stabilized export journey passed Chromium 1/1 in 10s and Firefox 1/1 in 13s; it proves mixed-latest CSV/provenance run IDs and a pinned-run bundle manifest, nested workflow SHA-256, exact member set, CSV bytes, and provenance. Three focused dev-router tests passed in 3s. `scripts/test check app` passed in 119s at patch-equivalent pre-rebase `691d1c7`: backend lint and 1,609 deterministic tests with 9 external deselections, frontend lint/typecheck/build and 1,297 tests, 7 logging-order tests, and 13 critical Chromium journeys. True thumbnail provisioning, native viewers/dialogs, external certification, and non-critical browser suites were intentionally outside that lane. Firefox exposed that Tab left a textarea active without a native change event; the permanent test now uses an ordinary adjacent-tab click and asserts the exact accepted draft PUT body and revision instead of relying on timing.
- Wave 5 headed confidence checkpoint: at `8574adc`, `scripts/test focus e2e tests/e2e/canvas-interactions.spec.ts --project=chromium --headed --grep "builds, runs, inspects, saves, and reopens a real workflow through the GUI"` opened visible Chromium and passed 1/1 in 19s. The journey GUI-creates, builds, executes, inspects exact results, saves, switches workflows, and reopens the identical accepted graph. This observable run is confidence evidence, not a replacement for the recorded headless Chromium/Firefox and completion lanes.

- Hot-reload fixture enforcement: the exact watcher test passed once in Chromium in 11s and Firefox in 13s at `353b425` plus its one-file diff. `scripts/test check frontend` passed lint, type checking, build, and 1,243 executed unit tests, but one unrelated `LoggerPanel.test.ts` suite hit a Vitest worker-loader timeout; its exact 14-test file passed in 2s without an edit. `scripts/test check browser` passed the changed hot-reload test and 53 other Chromium tests before an unrelated execution-lock failure; that exact workflow-interface test passed in 14s in a fresh runtime, and the one test not run after fail-fast passed separately in 11s. `scripts/test check docs` passed in 3s. Initial sandbox attempts that could not fetch documentation dependencies or bind local browser servers were blocked before collection. The task commit is discoverable with `git log -1 -- frontend/tests/e2e/hot-reload.spec.ts`.
- ISSUE-001: the new exact source-refresh selectors and the whole six-test file passed. `scripts/test check backend` passed in 82s at `6d42c05` plus the settled backend diff: 1,577 deterministic tests passed, 9 deselected, and 7 logging-order tests passed; external certification was intentionally omitted by the lane. The repair commit is discoverable by its service path.
- Sequential cache/output fixtures: the shared-downstream exact selector passed in 2s and its full 13-test file passed in 3s; the cache-parity and output-template files passed 10 tests in 5s. The settled `scripts/test check backend` result above covers all three files. The task commit is discoverable with `git log -1 -- backend/tests/test_services/test_cache_clearer.py`.
- Valid fallback recovery graph: the exact journey passed in Chromium in 14s and Firefox in 17s at `87fe704` plus its one-file diff. The settled frontend completion passed lint, type checking, 1,257 tests, and build in 30s before later unrelated T04 test edits. A subsequent settled-tree frontend completion passed lint, 1,260 tests, and build but exposed only ISSUE-003 test-mock type errors; the corrected exact type-check passed. The Chromium completion run passed the changed recovery journey before later failing from ISSUE-004 lock contamination; exact failed and fail-fast-deselected workflow-interface cases passed separately. `scripts/test check docs` passed in 2s. The task commit is discoverable with `git log -1 -- frontend/tests/e2e/graph-persistence.spec.ts`.
- ISSUE-003 ToolsPanel routing and the revised catalog interaction contract, based on `df883c0` plus this bounded task diff: single-click now toggles the bottom documentation panel, double-click creates exactly one node, and catalog secondary actions are isolated from both row gestures. The focused ToolsPanel unit file passed 64 tests in 14s; the exact browser journey passed once in Chromium in 64s and Firefox in 26s; `scripts/test check frontend` passed lint, type checking, build, and all 1,273 unit tests in 37s; `scripts/test check cross-browser-smoke` passed 10 Chromium and 10 Firefox cases in 67s; and the final `scripts/test check docs` passed in 4s. No cases were skipped, deselected, or retried. The first post-edit documentation recheck was blocked before collection because sandbox DNS could not fetch `myst-parser`; the permitted rerun above succeeded. The task commit is discoverable with `git log -1 -- frontend/src/components/panels/ToolsPanel.vue`.
- ISSUE-004 ordinary mutation contention: three exact deterministic regressions passed in 3s. `scripts/test check app` passed in 140s: backend lint, 1,590 deterministic tests with 9 deselected, 7 logging-order tests, frontend lint/type-check/build, 1,260 unit tests, and 10 critical Chromium journeys. `scripts/test check browser` then passed all 57 Chromium tests in 106s, including the formerly failing workflow-interface case at position 56. The task commit is discoverable with `git log -1 -- backend/src/bioimageflow_server/services/execution.py`.
- T05 backend campaign scope: `scripts/test focus backend --campaign-local` collected 1,599 tests, selected 1,489, and passed 1,480 with 9 external tests skipped and exactly 110 audited exclusions in 78s. The batch was also present for ISSUE-004's successful `scripts/test check app` and full Chromium completion, so the ordinary unscoped collection and cross-stack lanes remain valid. The task commit is discoverable with `git log -1 -- backend/tests/campaign_scope.py`.
- T05 frontend/browser campaign scope: the isolated worktree commit was reviewed, corrected to reject duplicate identities and unsupported partial selectors, and integrated as `f2eb9d8`. On main, `scripts/test check all` passed in 228s with backend lint, 1,590 deterministic tests and 9 deselected, 7 logging-order tests, frontend lint/type-check/build, all 1,271 unit tests, and all 58 Chromium journeys; external certification and Firefox execution were intentionally omitted by that lane. `scripts/test focus unit --campaign-local` passed 1,227 selected tests with exactly 44 audited skips in 29s. `scripts/test focus e2e --campaign-local --list --project=chromium --project=firefox` passed in 2s and independently reported 56 selected and 2 excluded cases for each browser.

## Completed revisions

Platform baseline for the latest code and audit is `fcb4408dd994a31567042b691eff85f3b268c5ab`.
Relevant platform commits: `f8c39de` Wetlands floor; `ea6abed` fail stale diagnostic/snapshot regressions explicitly; `b8a805c` drag readiness and pre-cleanup evidence; `306737f` published library adoption/contracts; `83ab981` fixture/worker certification; `fcb4408` GUI whole-DataFrame journey.
`c675e4a` and `11907b3` establish the original campaign and separate library release CI from platform scope.

The earlier sibling library baseline was `bb74097` in `../bioimageflow`, with tags `bioimageflow-core-v0.3.1` and `bioimageflow-v0.7.1`.
Library fixes include constant-only DataFrame parameters, annotation resolution, installed-version fixtures, synchronized failure-selection regression, and flushed log fixture output.
Direct remains supported.
[Required library CI](https://github.com/Inria-SAIRPICO/bioimageflow/actions/runs/34349497763) passed 12 jobs; [publication](https://github.com/Inria-SAIRPICO/bioimageflow/actions/runs/34349799812) succeeded.
The current sibling release is `659acda` with tag `bioimageflow-v0.7.2`; the platform adoption environment verified the regular installed `bioimageflow==0.7.2` wheel from its independent worktree environment alongside `bioimageflow-core==0.3.1` and Wetlands 2.4.1.
The old library task worktree was removed; do not reference it or share environments across worktrees.

## Retained validation evidence

These are historical results from `11907b3` plus the changes subsequently committed through `fcb4408`, not tests rerun during the inventory or handoff.
For all five rows below, the recorded code identity is the uncommitted tree based on `11907b3` whose completed changes are now in `306737f`, `83ab981`, and `fcb4408`; no per-run tree hash was preserved.
Compare the relevant paths against `fcb4408` before retaining these results; if their applicability cannot be established, rerun the exact relevant check instead of assuming certification.
Commands ran with `UV_NO_SYNC` and `BIOIMAGEFLOW_USE_LOCAL_CORE` unset and the temporary platform uv cache.
Durations are runner wall time.

| Command | Result | Duration |
| --- | --- | --- |
| `scripts/test focus backend tests/test_integration/test_platform_fixture_contracts.py::test_processing_fixture_runs_in_real_sequential_wetlands_worker --run-external` | 1 passed, no cleanup warning | 39s |
| `scripts/test focus backend tests/test_integration/test_platform_fixture_contracts.py tests/test_integration/test_real_execution.py` | 7 passed, external worker skipped here and passed separately | 5s |
| `scripts/test focus e2e tests/e2e/canvas-interactions.spec.ts --project=chromium --project=firefox --grep 'new dynamic tools connect cleanly'` | 2 passed | 24s |
| `scripts/test focus e2e --project=chromium --project=firefox --grep @critical --grep-invert 'managed execution resolves paths, retains its run, downloads results, and cleans up'` | 18 passed, managed journey excluded | 52s |
| `scripts/test certification` | 8 passed, 1,576 deselected; common-tools 0.2.0 | 13s |

Earlier scoped backend completion passed 1,454 deterministic tests, 19 deselected, and 7 logging-order tests in 73s; five additional local-runtime cases passed in 3s.
Earlier frontend completion selected 119 files and passed 1,213 tests with six excluded cases in 19s; lint, type checking, build, and documentation also passed.
Those selections used temporary command files/broad exclusions and are not a reproducible complete local-profile certificate.
Do not reuse their counts as proof of complete coverage; T05 must establish audited per-case selection.
Temporary `/private/tmp/bif-published-*.log` and `/private/tmp/bif-platform-*.log` may exist locally but are not required to resume.
Older unused runner/evidence drafts under ignored `backend/.pytest_cache/unfinished-test-plan/` are unfinished and must not be restored as functioning infrastructure.

For every new completion, append or replace the relevant task evidence with the exact command (including selectors/environment), source revision or diff identity, result, wall duration, skips/deselections/retries, external blockers, and resulting commit.
Preserve successful unchanged phases after a late failure; rerun only invalidated checks.
Before session handoff, update active ownership, dirty paths, issue dispositions, and the next action; archive resolved detail as concise commit references.

## Handoff validation

`scripts/test check docs` passed in 2s at `fcb4408` plus this documentation-only handoff diff, after a Sol/medium review of the restart protocol.
An initial sandbox attempt was blocked fetching a Sphinx dependency; the network-enabled run then exposed two out-of-tree specification links, which were corrected before the passing check.
No application, browser, or library tests were run for this documentation-only change; no such phase is claimed as newly certified.
The final handoff commit is discoverable with the path-specific Git command above.
