---
orphan: true
---

# Workflow coverage inventory

This is an assertion-level audit of workflow lifecycle, drafts, recursive editing, provenance, and owned-source transport at revision `fcb4408dd994a31567042b691eff85f3b268c5ab`.
The audit reads test bodies and their fixtures; it does not certify that the listed tests passed in this campaign.
No tests were executed for this audit.
Parallel scheduling, HPC, Parsl, and distributed execution are excluded.
Serialized `execution: parallel` values in persistence-only fixtures do not demonstrate parallel execution and should not cause unrelated lifecycle tests to be excluded automatically.

The unconfirmed identity concern below is queued for Astra review under the revised [campaign protocol](platform-test-plan.md); current status is tracked in [Campaign issues](platform-test-issues.md).
Remaining gaps are proposed work, not implemented coverage.

## Selector conventions

Backend selectors below are relative to `backend/tests/` and use `file.py::test_name` after `scripts/test focus backend tests/`.
Browser selectors are relative to `frontend/tests/e2e/` and use `file.spec.ts` with the exact quoted test title supplied to `--grep`; run through `scripts/test focus e2e tests/e2e/<file> --project=chromium` first, followed by Firefox after the exact case passes.
Frontend unit selectors use their full path relative to `frontend/` plus the quoted `-t` title after `scripts/test focus unit`.
Implementation filenames without a prefix refer to `backend/src/bioimageflow_server/services/`.
Specification references are to repository-root `platform_specs_v2.md`, with `platform_specs_v1.md` sections where relevant; these internal specifications are outside the Sphinx documentation tree.

## Lifecycle and durable edits

| Behavior and specification | Implementation owner | Inspected selectors and assertions | Negative coverage and remaining gap |
| --- | --- | --- | --- |
| Create one canonical saved document; graph owns visible name (§2, §12) | `workflow_store.py` | `test_services/test_workflow_store.py::test_create_persists_one_canonical_document` checks exact envelope keys, description, graph names, results path; `::test_save_and_get_use_the_graph_as_display_authority` checks graph round-trip and no duplicate metadata display name. | `::test_unknown_persisted_sections_are_rejected_without_fallback` injects an extra persisted section and expects validation failure. |
| Create from dialog with generated identity (v1 §3.8) | Frontend workflow dialog and workflow store; `workflow_store.py` | `workflow-crud.spec.ts`, “creates a workflow from the polished dialog” fills visible name, checks generated name, submits, and confirms the backend list contains exact ID/label. | Browser collision rejection and cancelling creation are not asserted in this journey. |
| Save As and switch workflow (v1 §3.8, v2 §12) | Frontend workflow store and open/save dialogs | `workflow-crud.spec.ts`, “save-as creates a copy and open dialog can switch workflows” creates an empty workflow, copies it, then opens original by dialog. | This browser case proves navigation and names, not graph/source independence; backend copy cases below cover stronger semantics. |
| Duplicate definition without results (§12) | `workflow_store.py` | `test_services/test_workflow_store.py::test_duplicate_assigns_new_definition_identity_atomically` checks original definition name, copy ID/label, missing copy results, preserved original result bytes. | `::test_duplicate_failure_does_not_publish_partial_copy` injects copy failure and checks destination and staging directory absence. |
| Copy captured agent graph including unsaved local sources (§12) | `workflow_store.py`, `workflow_draft.py`, frontend conflict UI | `test_services/test_workflow_store.py::test_duplicate_agent_snapshot_copies_unsaved_sources` checks source record bytes and ownership, saved original unchanged, and original draft revision unchanged for both parameter values; `agent-draft-sync.spec.ts`, “saves the agent snapshot as a copy (keep canvas first: false)” and corresponding `true` case check exact copied graph and unchanged original draft. | Browser fixtures only change an empty graph's label and mark dirty through internal store access; recursive source transfer is not exercised through this UI. |
| Reject stale duplicate generation (§12) | `workflow_store.py` | `test_services/test_workflow_store.py::test_duplicate_rejects_recreated_source_identity` deletes/recreates the original then submits the captured generation; expects conflict and no copy directory. | This protection must not be assumed for source-update tokens; see paused concern. |
| Durable generation survives restart (v1 §2.4.2, v2 §12) | Workflow store generation ledger | `test_services/test_workflow_generation_ledger.py::test_unchanged_workflow_keeps_its_generation_after_restart` checks stable generation; `::test_delete_tombstone_and_stale_token_survive_restart` checks increasing tombstone/recreation generations and rejected old token after restart. | Service restart is reconstructed in process; packaged application crash recovery remains a separate acceptance boundary. |
| Move carries results, preserves definition/hash, rewrites provenance (§10, §12) | `workflow_store.py` | `test_services/test_workflow_store.py::test_move_changes_workspace_identity_not_definition_name_or_hash` checks moved result bytes and unchanged definition/hash; `::test_move_rewrites_saved_workspace_provenance_without_changing_hash` checks the parent source ID and hash after move. | These are service-level prepared moves; browser folder move/rename with open editors is not established by these cases. |
| Delete exact tab and persist empty last-workflow state (§12) | Frontend workflow store, App tab coordination | `workflow-crud.spec.ts`, “deleting A closes its exact tab, activates B, and cannot recreate A on reload” checks tabs, exact surviving title, GET 404, and no implicit POST; “deleting the last workflow leaves a non-persistent empty state across reload” checks placeholder, zero tabs, GET 404, no implicit creation. | Browser tests exercise successful confirmation; cancellation and stale-generation deletion are separate unit/API obligations. |
| Draft writes carry revision, dirty status, validation (§12; v1 §2.2) | `workflow_draft.py`, draft router | `test_routers/test_workflow_drafts.py::test_put_writes_atomic_draft_and_conflicts_on_stale_revision` checks accepted revision, dirty flag, invalid-tool validation result, and disk graph; stale write returns 409 with expected/current revisions. | Its accepted graph intentionally has missing tool metadata and is not an executable fixture. |
| Explicit discard restores saved graph atomically (§12) | Draft reset route and frontend close flow | `test_routers/test_workflow_drafts.py::test_reset_to_saved_is_revision_checked_and_publishes_only_on_success` checks restored node, revision, clean flag, exact event, then unchanged disk bytes and no second event on stale reset. | `::test_reset_to_saved_rejects_locked_and_missing_workflows` checks 423 and 404. |
| Agent edits update active clean canvas without reload (§12) | Draft events, frontend WebSocket consumer | `agent-draft-sync.spec.ts`, “backend operation edits appear on the active clean canvas through WebSocket” submits a real draft operation, observes its actual WebSocket message, checks node visibility and absent conflict, and asserts no main-frame navigation. | Dirty-canvas copy tests above use internal dirty-state setup; add a visible parameter-edit conflict journey rather than claiming entirely UI-created conflict coverage. |
| Browser fallback graph recovery (v1 §2.2) | `useAutoSave`, root canvas recovery | `graph-persistence.spec.ts`, “auto-saved graph restores nodes and edges after reload” asserts the real seeded `SeedNumbers` and `IncrementNumbers` schemas, validates their whole-DataFrame graph through the backend, manually seeds IndexedDB, and checks exact workflow identity, dirty state, nodes, edge ID, and a nonempty non-NaN SVG path after reload. | This is fallback rendering evidence, not backend-draft recovery or executable-result validation. |

## Recursive editing and interfaces

| Behavior and specification | Implementation owner | Inspected selectors and assertions | Negative coverage and remaining gap |
| --- | --- | --- | --- |
| Stable field/output publication and renaming (§5) | Nodes panel, interface graph utilities | `workflow-interface.spec.ts`, “exposes and renames stable interface ports” publishes both directions, checks exact internal targets/source, then input rename preserves ID. | No connected parent edge is present; this does not certify compatible connection preservation during nested apply. |
| Multiple DataFrame publication slots compact together with edges (§5) | Nodes panel and positional slot utilities | `workflow-interface.spec.ts`, “publishes multiple DataFrames and compacts slots without changing surviving IDs or edges” creates three publications, physically connects a whole-DataFrame edge, unpublishes middle slot, exercises Undo/Redo, verifies surviving IDs and shifted edge index, then reloads. | Boundary tests should separately prove published slots reject competing edges and non-upstream sources do not expose publication. |
| Publish child DataFrame port through enclosing interface (§5, §7) | Nested canvas save and Nodes panel | `workflow-interface.spec.ts`, “publishes an existing child DataFrame port through the parent interface” opens child, publishes child input, saves to parent, publishes by child stable ID, then unpublishes and saves empty enclosing interface. | Does not reload while the nested edit remains private, or reject a stale parent apply. |
| Delete exposed node prunes interface before autosave (§5) | Canvas mutation utilities and draft persistence | `workflow-interface.spec.ts`, “deleting an exposed node removes its interface references before autosave” waits for accepted draft 200, checks no persistence issue, saves, and checks exact remaining node and empty outputs. | Shared multi-target inputs are not covered by this browser fixture. |
| Group selected graph with detached branch (§6.1) | Frontend `utils/workflowGrouping.ts` | `src/utils/__tests__/workflowGrouping.test.ts`, “preserves detached branches and creates stable field and DataFrame ports” checks both selected nodes preserved, incoming field target and rewired stable input handle, plus outgoing DataFrame handle; “supports a zero-interface selection” checks empty interface. | Despite its title, the first case does not assert an incoming positional DataFrame port; add that specific case. |
| Group via GUI (§6.1) | Canvas context menu | `workflow-interface.spec.ts`, “groups a selected tool into an ordinary workflow node” groups one node, saves, checks wrapper discriminator and embedded tool ID. | Multi-node incoming/outgoing rewiring, undo as one transition, and recursive clipboard transport need distinct browser journeys. |
| Nested snapshot ownership and durable isolation (§7) | `nested_workflow_snapshot.py` | `test_services/test_nested_workflow_snapshot.py::test_open_is_idempotent_and_root_owners_do_not_collide` checks reopened same session and separate roots; `::test_nested_owner_identity_is_hierarchical` checks child session owner and scoped open-editor queries; `::test_complete_graph_and_interface_survive_service_restart` checks exact recovered graph and stable input ID/name. | MissingTool graph intentionally tests persistence, not execution. Private nested reload in browser remains unproven. |
| Nested CAS and descendant cleanup (§7) | `nested_workflow_snapshot.py` | `test_services/test_nested_workflow_snapshot.py::test_replace_and_delete_are_revision_checked` checks increment then stale PUT/DELETE errors; `::test_parent_delete_requires_descendant_cleanup` checks reported child session before ordered cleanup; `::test_root_cleanup_removes_exact_snapshot_tree` preserves unrelated root snapshot. | These do not test parent graph reconciliation or destructive interface confirmation. |
| Nested conflict waits for explicit user choice (§7) | Frontend `sessions/nestedSnapshotPersistence.ts` | `src/sessions/__tests__/nestedSnapshotPersistence.test.ts`, “holds a revision conflict and rebases the latest local graph only after an explicit action” uses a mocked 409, proves no automatic retry, then explicit keep-local fetches latest and writes with its revision. | Mock transport validates coordinator policy, not rendered conflict dialog or backend race. |
| Semantic operation batch atomicity (§8, §12) | `workflow_draft_operations.py` | `test_services/test_workflow_draft_operations.py::test_operation_batch_is_atomic_on_failure` creates a duplicate node in second operation, checks error index 1 and original graph remains empty. | `::test_deleting_nested_input_prunes_parent_edge_and_binding_atomically` checks removed port and edge, but starts with empty bindings; its name overstates binding-removal assertion coverage. |
| Containment cycle rejection (§6) | `workflow_store.py` | `test_services/test_workflow_store.py::test_direct_and_transitive_source_containment_cycles_are_rejected` constructs A inside B then attempts B inside A and checks scoped cycle error. | This body exercises transitive save rejection, not every entry point named in the specification (paste/import/duplicate/move/source update). |

## Provenance, sources, and archive transport

| Behavior and specification | Implementation owner | Inspected selectors and assertions | Negative coverage and remaining gap |
| --- | --- | --- | --- |
| Source update is explicit, preview is identity-bound and nonmutating (§10, §12) | `workflow_sources.py` | `test_services/test_workflow_sources.py::test_source_refresh_is_previewed_and_applied_explicitly` saves a new source label, checks parent remains old after preview, then apply replaces child label and provenance hash; `::test_source_refresh_conflicts_when_destination_is_recreated_with_same_artifact` proves an old token rejects an exactly recreated same-ID destination without changing its graph, hash, generation, or files. | `::test_source_refresh_conflicts_when_parent_changes_after_preview` checks hash-changing parent edits; `::test_source_refresh_preview_captures_parent_and_generation_under_one_lock` proves lifecycle mutation cannot interleave the captured document and generation. Workspace switching and source-workflow recreation remain separate unconfirmed boundaries. |
| Detach only changes provenance (§10) | Semantic graph operations | `test_services/test_workflow_draft_operations.py::test_detach_source_changes_only_provenance` compares embedded graph before/after and checks source becomes null. | Open nested editor exclusion, UI context action, and detached source deletion journey remain gaps. |
| Owned source imports materialize editable files (§6.2) | `workflow_artifacts.py`, `workflow_store.py`, graph builder | `test_services/test_editable_workflow_sources.py::test_import_materializes_editable_file_and_immediate_run_uses_saved_bytes` checks source path, manifest excludes executable text, old storage absent, and exact values change from 2/3/4 to 21/22/23. | Archive adapter is fake; compute uses DataFrameTool with `dev_mode=True`, so this is not worker isolation certification. |
| Captured code and independent import remain stable (§9) | Graph compilation and owned-source loader | `test_services/test_editable_workflow_sources.py::test_execution_snapshot_and_other_import_remain_independent` builds before editing, computes captured old values, then fresh edited values, then unchanged second import values. | Explicit local DataFrame execution; no worker PID assertions. |
| Copy/export capture edited sources (§6.2, §12) | `workflow_store.py`, owned sources | `test_services/test_editable_workflow_sources.py::test_duplicate_and_export_include_edits` executes copied edited values, checks fake export adapter receives edited source, edits copy back, and verifies original still edited. | Pair with a real archive round-trip journey; fake adapter does not certify ZIP layout. |
| Bound metadata and invalid edits (§6.2, §8) | Validator and source loader | `test_services/test_editable_workflow_sources.py::test_scoped_metadata_comes_from_executed_source` checks node output schema and absent global catalog registration; `::test_invalid_edit_blocks_execution_instead_of_reusing_old_code` writes invalid Python, checks validation false and source filename in diagnostic. | The invalid-edit test does not actually submit execution after the edit; add explicit refused run and unchanged outputs if not covered elsewhere. |
| Import opens its own source; embedding prepares independent source (§6.2) | Editor node route, prepare-embedding route, canvas drop | `editable-import.spec.ts`, “imported node opens its own script and immediately executes saved edits” performs real export/import, clicks script open, checks destination path, edits file and runs exact CSV values, then dispatches a workflow drop and checks new destination source bytes and draft binding. | File edits use Node filesystem API, editor command is `true`, drag uses synthetic drop, and results are checked on disk. Real external editor/native drag remain human acceptance; embedding final edit is not executed. |
| Import collision rename preserves root/child naming contract (§6.2) | Workflow import router/store and rename dialog | `test_routers/test_workflows.py::test_import_workflow_archive_conflict_and_name_override` uses fake archive adapter, checks 409 suggestion, renamed root definition/display names, untouched child/original names, and no-override preservation; `workflow-crud.spec.ts`, “import collision rename persists the chosen visible name” uses a real exported archive and checks title/tab/tree after reload. | Real browser archive is empty; recursively owned executable source round-trip belongs to the separate editable-import journey. |
| Portable export and results bundle distinction (§12) | Workflow store and archive adapter | `test_services/test_workflow_store.py::test_portable_export_is_generated_from_the_accepted_graph` checks library-shaped data delivered to fake adapter; `::test_results_bundle_is_rejected_before_archive_adapter_read` constructs marker ZIP and checks rejection with no destination. | This row excludes results materialization, pinned-run semantics, and folder replacement, which need the execution/results inventory. |
| Trusted Python authoring uses fresh helper manifest (§11) | `workflow_sources.py` | `test_services/test_workflow_sources.py::test_python_materialization_uses_workflow_local_factory_and_fresh_helpers` writes entry/helper, previews/applies, checks definition and authoring provenance, edits helper, checks new manifest hash and replacement label. | `::test_python_materialization_is_disabled_in_webapp_mode` checks permission failure; changed-after-preview manifest, exactly-once call counter, symlink rejection, and ordinary Run never importing authoring source require dedicated assertions. |

## Resolved identity concern (ISSUE-001)

GPT-6 Astra/high reproduced an old generation-1 source-update token mutating an exactly recreated generation-3 destination whose graph and artifact hash matched the preview baseline.
The specialist established that v2 §§10 and 12 unambiguously require rejection, and classified a destination-generation check under the existing mutation lock as `safe-to-fix`.
Prepared source-refresh and Python-rebuild operations now capture the destination generation atomically with the previewed document and reject any generation change before staging or writes.
The new regressions cover same-content recreation and deterministic lifecycle serialization; workspace switching and source-workflow recreation were not reproduced and are not claimed as fixed defects.

## Priority after resumption

Prioritize nested private-edit reload/discard and stale-parent application with connected stable ports; source-update destructive confirmation and conflict journeys; and recursive source copying through the agent-copy UI.
Refactor overstated test names or add the missing assertions for nested binding deletion and incoming DataFrame grouping.
Keep real desktop dialogs, external editor interaction, operating-system drag-and-drop, and application crash/restart acceptance explicitly separate from headless browser and in-process service checks.
