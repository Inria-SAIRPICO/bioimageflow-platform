---
orphan: true
---

# Runtime and integration coverage inventory

This is a partial inspected coverage inventory, not a passing certification report.
The audit baseline is `fcb4408`; no tests were executed during this inventory pass.
The campaign paused on an identity-generation issue reported by the workflow audit before the proposed runtime test changes began.
Selectors below are relative to `backend/tests/` unless prefixed with `frontend/`; use `scripts/test focus backend tests/<selector>` for backend cases.
File-level selectors identify a relevant family, while named selectors identify assertions inspected directly or a specific follow-up boundary.
Parallel scheduling, HPC, Parsl, managed remote execution, and distributed engines are excluded.
An in-process Direct computation and a mocked worker lifecycle are distinct from real sequential Wetlands worker certification.
Every row declares its stable feature ID directly; that ID is the sole mapping key to the [obligation dashboard](platform-test-dashboard.md), while labels and row positions are descriptive only.
The `Scenario contracts` cell records all five dashboard dimensions with the dashboard state: applicable cells delegate to the row's named behavior and evidence/gap prose, while each `N` repeats its concise non-applicability reason.

## Execution, cache, and outputs

| Stable ID | Behavior and authority | Implementation owner | Existing evidence and negative boundary | Gap or next acceptance step | Scenario contracts |
| --- | --- | --- | --- | --- | --- |
| RT-EXE-001 | Run accepted draft; reject stale revision or unequal graph (v1 §2.4.5, v2 §§8–9) | `routers/execution.py`, execution manager, draft coordinator | Router selectors `test_run_selected_uses_accepted_draft_graph`, `test_run_rejects_stale_draft_before_execution_side_effects`, and `test_run_rejects_same_revision_graph_mismatch` bind accepted graphs and reject before manager start. Manager `selected_run` regressions cover full versus selected diagnostics, selected/upstream/unrelated workflow scopes, empty/unknown/unresolved/mixed targets, global errors, missing compilation, and dangling edges. Browser `execution.spec.ts`, “Run Selected executes a valid branch while an unrelated invalid branch remains unexecuted”, proves the exact request, valid upstream execution and rows, untouched unrelated node, absent unrelated cache/result, and complete accepted graph. | The selected valid-branch contract is verified in Chromium and Firefox at `8908388`; selection expansion through more complex recursive interfaces remains part of nested-workflow coverage. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-002 | Preserve identity across compilation and storage retries (v1 §2.4.5, v2 §12) | execution router/manager | `test_routers/test_execution_router.py::test_run_cannot_accept_same_id_replacement`, `::test_run_storage_retry_retains_one_pending_execution_identity`; explicit concurrent replacement/retry arrangements. | Include this family in per-case local selection even though neighboring execution-runtime tests cover remote features. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-003 | Prevent a second run, lock mutation during execution, and serialize ordinary mutations (v1 §2.4.5; v2 §7) | execution manager/router | `test_routers/test_execution_router.py::test_run_conflict_returns_409`, `::test_clear_while_running_returns_423`; `test_services/test_execution_manager.py::test_idle_mutation_leases_serialize_without_execution_conflict`, `::test_idle_mutation_lease_rejects_an_actual_execution`; `test_routers/test_workflows.py::test_save_waits_for_admitted_draft_validation_then_succeeds` holds real draft validation, proves Save waits, Run rejects, and Save succeeds after release. | Browser currently proves successful source execution and the formerly intermittent child-port Save path, not a visible mutation-lock attempt during a held execution. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-004 | Real Direct source execution (v2 §9) | graph builder/execution manager | `test_integration/test_real_execution.py`; `frontend/tests/e2e/execution.spec.ts` creates a workflow in GUI, sets source via API while unmounted, runs via GUI, checks backend completion and executed class; `canvas-interactions.spec.ts`, “builds, runs, inspects, saves, and reopens a real workflow through the GUI” creates and connects real `SeedNumbers` and `IncrementNumbers` nodes only through controls, runs the accepted graph, and checks exact visible merged rows. | The main browser journey certifies ordinary Direct GUI execution, not worker process isolation; real sequential Wetlands evidence remains separate below. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-005 | Recursive Direct execution and scoped result attribution (v2 §§5,7,9,13) | recursive graph compiler, execution manager, result projection | Browser `nested-execution.spec.ts`, “executes nested Direct work and preserves scoped results after save and reopen”, runs a real root→nested→root DataFrame chain through the GUI, checks `nested_step/nested_increment` status and projection identity, exact nested rows `2,3,4`, exact downstream rows `(2,3),(3,4),(4,5)`, durable results after Save/reload/reopen, and sibling isolation. | Verified in Chromium and Firefox at integrated `fd96f8f`; this DataFrame-only Direct journey does not certify worker isolation, nested cancellation/failure, or recursive owned-source execution. | S=V: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-006 | Real sequential worker isolation and files (library §5, v2 §9) | execution/compiler, Wetlands public API | `test_integration/test_platform_fixture_contracts.py::test_processing_fixture_runs_in_real_sequential_wetlands_worker` proves distinct PID, transformed values, and output files. `test_integration/test_held_worker_fixture.py` validates the cancellation fixture's exact worker origin, sequential Wetlands graph, cooperative acknowledgement, and absent output. | External opt-in environment creation remains separate from the dev-only browser fixture; do not replace either claim with a DataFrame-only graph. | S=V: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-007 | Progress and completion retain one context (v1 §2.4.5) | execution manager, WebSocket bus | Manager tests retain controlled context checks. Browser `execution-cancellation.spec.ts` runs a real sequential Wetlands worker: while held, a live `node_state` event and visible running banner identify the exact rerun/workflow/draft revision; after release, zero-based `row_complete` progress and `execution_complete` carry the same context. The terminal status and GUI result remain attributed after reload, and the canceled prior run has a distinct ID. | Real worker context, cancellation/rerun separation, and reload passed Chromium and Firefox at `2c1ff72`. BioImageFlow 0.7.2 emits row-completion events while collecting completed tasks after the batch, not while the next task is held; do not claim live per-row streaming. Refusal of malformed or stale progress remains a separate gap. | S=V: real node-state, row progress, completion, status, and GUI result agree; R=G: refuse an invalid or stale progress event without unintended projection; F=V: canceled run and successful rerun retain distinct context; P=V: reload retains the terminal run identity and result; B=V: each observed rerun event carries the exact execution/workflow/draft revision |
| RT-EXE-008 | Failure, retry, and stop retain completed output (v1 §2.4.5; library §§9,13) | execution manager | Manager regressions cover cancellation preservation and retry identity. Browser `execution-cancellation.spec.ts` uses socket state rather than sleeps to hold a real sequential worker, verifies Stop reaches cooperative cancellation before idle, retains exact completed-source rows, exposes no partial worker result, preserves workflow/revision/run identity across reload, and reruns to exact values in a distinct worker process. | Cancellation is verified in Chromium and Firefox at integrated `da2ced2` with BioImageFlow 0.7.2. Add the corresponding actual worker failure, visible diagnostic/log, correction, and rerun journey; broader retry variants remain lower-level. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-009 | Cache invalidation propagates downstream, selected clears take priority (v1 §2.4.5; library §6) | `services/execution.py::clear_node_cache`, output-view service, NodePanel | `test_services/test_cache_clearer.py::test_clear_propagates_out_of_date_to_downstream`, `::test_cleared_node_takes_priority_over_out_of_date`, `::test_invalid_graph_does_not_clear_existing_cache`; `::test_clear_multiple_nodes_shared_downstream` builds a valid two-input consumer and asserts both cleared sources are `unexecuted` while their shared descendant is `out_of_date`. `test_output_views_clear.py` verifies scoped latest pointer/materialization removal with a surviving sibling. Browser `everyday-node-editing.spec.ts` proves root bulk Clear confirmation/cancel, exact graph/revision preservation, selected output removal across reload, accepted-draft gating, zero clear requests on persistence failure, and retained results after refusal. Its two-node Direct journey additionally proves source `unexecuted`, dependent `out_of_date`, selected result 404, downstream retention, unchanged graph/revision, exact rerun rows, and same-process browser reload badges. The backend restart integration selector reproduces the stale status before the fix and verifies fresh HTTP GET from a separate Python process on the same workspace after Clear; identity/race regressions protect accepted draft fields. | Selected Clear and downstream stale-result presentation are verified in Chromium and Firefox at `30faeb6` and `fe0cf9d`; process-level backend-restart status projection and GUI confirmation/rerun are verified at `0043397` and `58520b5`, resolving ISSUE-011. A single Playwright session was not carried across backend restart, and nested-canvas scoped clearing remains a later recursive boundary. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content across a backend restart using process-level HTTP plus separately verified GUI projection; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-010 | Validation cache flags agree with actual computation (library §6) | graph validator/builder | `test_services/test_cache_parity.py` executes explicitly sequential Direct DataFrame tools and validates before/after seed and parameter edits. | This is in-process Direct cache evidence, not worker serialization or isolation. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-011 | Output template chooses file name and changes signature (library §7; v1 §3.5) | library translation and output template UI | `test_services/test_library_output_templates.py::test_workflow_from_dict_uses_node_output_template`, `::test_output_template_changes_execution_signature`; the explicitly sequential Direct fixture writes a real asset and checks a distinct cache record directory. | Browser template editing plus actual resulting filename is missing; no worker acceptance is claimed. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-012 | Latest projection chooses symlinks or pointer fallback (v2 §12; library §7) | `services/output_views.py` | `test_services/test_output_views.py::test_auto_uses_symlinks_when_supported`, `::test_auto_falls_back_to_pointer_without_copying`, `::test_runtime_symlink_failure_falls_back_to_pointer`; capability and export calls mocked. | Real platform-specific filesystem capability belongs in release acceptance; do not claim mocked symlink denial proves Windows behavior. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=U: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-013 | Read exact latest record and reject incomplete data (v1 §2.4.4; library §6) | `services/result_store.py` | `test_services/test_result_store.py::test_get_latest_dataframe_loads_library_generated_v1_view`, `::test_get_latest_dataframe_raises_not_ready_for_zero_byte_parquet`, `::test_zero_byte_latest_hash_does_not_fall_back_to_stale_data`, `::test_malformed_node_id_does_not_probe_outside_latest`. | Audit legacy-link fallback separately before removal; existence of a legacy-named test alone does not authorize deleting currently specified library read behavior. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-014 | Latest-results export copies assets and rolls back failed replacement (v2 §12) | `services/workflow_exports.py`, export dialog | Service tests retain real-filesystem materialization, rollback, and symlink-boundary coverage. Browser `result-exports.spec.ts` performs a real full run followed by a selected rerun, downloads Latest results through the GUI, parses the ZIP, and proves exact CSV bytes plus provenance from the two distinct latest node runs. | The real GUI mixed-latest archive is verified in Chromium and Firefox at `7ab16e6`; rollback remains lower-level service coverage and native save-dialog presentation remains a manual boundary. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-EXE-015 | Workflow-results bundle pins one successful run (v2 §12) | workflow exports/router and export dialog | Service tests retain unavailable/import-boundary coverage. Browser `result-exports.spec.ts` downloads Workflow with results after two real GUI runs, parses the outer and nested archives, verifies the manifest run ID and workflow SHA-256, proves only the selected second-run node is present, and checks its exact CSV/provenance bytes. | The pinned-run bundle is verified in Chromium and Firefox at `7ab16e6`; archive import round-trip and native save-dialog presentation remain separate obligations. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |

## Tables, datasets, images, and viewers

| Stable ID | Behavior and authority | Implementation owner | Existing evidence and negative boundary | Gap or next acceptance step | Scenario contracts |
| --- | --- | --- | --- | --- | --- |
| RT-RES-001 | Single-node pagination, filtering, sorting, CSV keep source row identity (v1 §2.4.4, v2 §13) | nodes/data-table routers, result and projection stores | Backend node and projection selectors prove filter-before-sort-before-page, exact source positions, and matching query/CSV behavior. Browser `canvas-interactions.spec.ts`, “filters, sorts, pages, and exports an exact real execution result”, creates and runs a real deterministic 60-row Direct source through the GUI, applies a visible `label starts with keep-` filter, sorts `score` descending, checks all correlated visible source-row/label/score values against exact `source_rows`, changes page size from 25 to 50, uses next and direct-page navigation with exact requests/ranges, and parses the complete downloaded merged CSV header and all 30 ordered rows. A second real GUI journey refuses incomplete numeric Between without changing 60 visible rows, then applies a complete range with exact source-row IDs and restores all rows through Clear filters. | The primary single-selection merged-table journey is verified in Chromium and Firefox at integrated `d1072ce`; numeric refusal/recovery is verified at `168b487`. Image action after sorting remains a separate obligation; multi-selection and durable widths are verified below. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-002 | Merge tables only when lineage preserves selected rows (v2 §13) | `services/data_table_projection.py` | Lower-level projection cases cover exact indices, parent expansion, filtered-anchor fallback, and divergent lineage. Browser `canvas-interactions.spec.ts`, “merges related selected results, falls back for unrelated data, and preserves widths”, GUI-creates and Direct-executes a connected Seed→Increment pair plus an independent result source; Shift-selection verifies exact request roles/order, merged source-node/source-column metadata and source rows, visible correlated rows, source navigation, then stacked fallback with exact independent rows. The strengthened journey asserts that unrelated selection refuses a merged table, instead shows separate source-pure tables with all 3 Seed and 60 independent rows, and repeats that fallback after reload before restoring related merge and the scoped width. | The real merged/stacked multi-selection and navigation journey is verified in Chromium and Firefox at `c050207`; exact unrelated-lineage refusal and reload are verified at `7d9de88`. Larger mixed selections remain covered by lower-level projection cases rather than Cartesian GUI duplication. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=N (projection selection is synchronous and has no cancellation or recovery lifecycle); P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-003 | Column labels, filters, and user widths precede sorting/paging (v2 §13) | projection service/data-table router, table-width composable | Projection/router cases verify label qualification, filter/sort/page order, and matching CSV. The real multi-selection journey resizes a merged column by exactly 50px, navigates to a different selection and back, verifies the scoped stored width is reapplied, then Reset restores measurement and removes the localStorage override. | Persistence/reset is verified in Chromium and Firefox at integrated `c050207`; deployment-specific storage denial remains a lower-priority boundary. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-004 | Upload partial success, size cap, filename and path boundary (v1 §2.4.10) | dataset router/store | `test_routers/test_datasets.py::test_upload_partial_success`, `::test_upload_oversize_mid_stream`, `::test_upload_dos_guard_rejects_huge_content_length`, `::test_upload_multi_file_total_over_per_file_cap`; store cleanup and sanitization tests. | Browser upload uses fake TIFF bytes and certifies file management only, never image readability. Add browser per-file failure plus successful sibling visibility. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-005 | Dataset folder operations preserve physical path and reject stale destructive preview (v2 §16) | dataset store/router/panel | `test_services/test_dataset_store.py::test_folder_rename_and_move_do_not_change_dataset_path`, `::test_folder_move_rejects_descendant_cycle`, `::test_recursive_delete_requires_current_preview_revision`; `frontend/tests/e2e/datasets-panel.spec.ts` checks upload, rename, drag creation, grouped moves and checkbox hierarchy. | Existing browser title wait is generic; use intended workflow identity when extending. Add reopen plus Files parameter snapshot assertions and confirmed delete consequences. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-006 | Cancel browser upload (v1 §2.4.10, v2 §16) | dataset upload client/panel | `frontend/tests/e2e/datasets-panel.spec.ts` “cancels a file dropped on the canvas while its upload is active” holds intercepted request, clicks Cancel, checks cancelled message and Retry. | Interception proves client UI cancellation, not server partial-file cleanup; store tests own cleanup independently. Retry round-trip is not asserted. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-007 | Image response identity, OME-TIFF conversion, offset JSON (v1 §2.4.8) | nodes router, result table, Avivator panel | Router tests retain conversion, offsets, unreadable-image, and cache-prune boundaries. Browser `avivator.spec.ts` GUI-creates and runs a typed image-and-Path result, verifies exact displayed paths and a readable preview, reads the workflow-scoped TIFF/OME/offset responses, clicks the actual result-table Avivator action, and verifies the loaded iframe URLs. | The primary real image/file viewer journey is verified in Chromium and Firefox at `7ab16e6`. Hermetic E2E intercepts thumbnail generation with a valid PNG, so true thumbnail-worker startup, multidimensional rendering, and native external viewers remain separate obligations. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-008 | Thumbnail placeholder, relative asset path, generator formats (v1 §2.6) | thumbnail manager/generator, nodes router | `test_routers/test_nodes.py::test_thumbnail_endpoint_signals_pending_for_placeholder`, `::test_thumbnail_endpoint_resolves_record_relative_asset_paths`; `test_services/test_thumbnail_generator.py` covers constant arrays, RGBA and canonical extension links with mocked reader boundaries. | Real thumbnail environment startup and appearance in GUI are external/integration acceptance, not certified by reader mocks. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-009 | Reveal and native dialogs (v1 §§2.4.7,3.13) | filesystem router, desktop bridge | `test_routers/test_filesystem.py::test_reveal_path_calls_reveal_function`, `::test_reveal_path_handles_os_error`; `test_desktop.py` mocks selection/cancel/drop paths and process launches. | Native OS dialog, physical drop, file browser launch require manual desktop acceptance. Deployment denial coverage is owned by deployment-mode tests, not this router file alone. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=N (native reveal and chooser actions are ephemeral); B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-010 | Fiji resolves workflow image and launches separate arguments (v1 §2.4.8) | Fiji router/launcher | `test_services/test_fiji_launcher.py::test_open_launches_image_as_separate_argument` captures exact argv with spaces; absent config/invalid launcher/unsupported CPU/process failure cases; `test_routers/test_fiji_router.py` owns mode and result-address boundaries. | Launchers are fake executable files and process callable is replaced. Actual Fiji install and displayed image require manual acceptance. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=N (launch is ephemeral; settings persistence is GUI-SHL-008); B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-RES-011 | Napari environment and reconnect lifecycle (v1 §2.4.8) | Napari manager/launcher/router | `test_services/test_napari_manager.py`, `test_services/test_napari_launcher.py`, `test_routers/test_napari_router.py` are existing focused families; exact assertion-level review remains pending. | Do not mark complete until bodies and mode gates are fully audited; actual Qt window/layers remain external/manual. | S=U: exercise the named successful behavior and assert the exact row outcome; R=U: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=U: exercise cancellation, failure, or recovery and assert coherent retained state; P=U: reload or reopen durable state and assert the row's specified identity and content; B=U: exercise the applicable identity, concurrency, ownership, or permission boundary |

## Events, diagnostics, agents, and desktop

| Stable ID | Behavior and authority | Implementation owner | Existing evidence and negative boundary | Gap or next acceptance step | Scenario contracts |
| --- | --- | --- | --- | --- | --- |
| RT-XCT-001 | WebSocket subscriptions, reconnect snapshot, slow-consumer isolation (v1 §2.5) | `ws/handler.py`, app wiring | `test_ws/test_handler.py::test_execution_messages_and_snapshot_share_one_context`, `::test_endpoint_sends_initial_status_snapshot_when_provider_configured`, `::test_queue_backpressure_drops_oldest`, `::test_slow_consumer_does_not_block_fast_consumer`; endpoint and queue tests. | Add browser disconnect/reconnect during held sequential execution and prove restored state belongs to originating workflow. Exclude only remote snapshot test by exact identity. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-XCT-002 | Execution logs carry provenance; background logs stay global | logging bridge/handler | `test_ws/test_logging_bridge.py::test_record_provenance_cannot_be_relabelled_by_a_later_execution`, `::test_unattributed_worker_record_is_not_claimed_by_a_later_execution`; `test_ws/test_handler.py::test_broadcast_log_preserves_execution_context_and_global_logs`. | v2 §1.8 and implementation commit `a8d9533` establish contextual execution logs; ISSUE-002 reconciled the stale v1 §2.5 table wording. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-XCT-003 | One contextual error in history and Logger, readable traceback (v1 §§3.7,3.11) | execution manager, frontend logging/error stores and panels | `frontend/tests/e2e/error-handling.spec.ts` retains synthetic transport/projection coverage. `execution-failure-rerun.spec.ts` drives a real sequential Wetlands worker failure and proves one node-attributed history row and one enriched Logger row with readable remote traceback, no partial result, retained upstream rows, unlocked GUI correction, persisted revision, successful rerun rows `4,8,12`, and exact output-file bytes. A manager regression rejects duplicate contextual ERROR publication when the preliminary failed progress event has no details. | Verified in Chromium and Firefox at integrated `4676ffb`; the app completion lane also preserves the existing cancellation journey. Packaged worker-process crash and dependency-provisioning failure presentation remain separate boundaries. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-XCT-004 | Structured HTTP error details and expected-error logging (v1 §2.4.1b) | error handler | `test_routers/test_error_handler.py::test_http_exception_preserves_declared_structured_details`, `::test_expected_forbidden_http_exception_is_not_warning_logged`, `::test_request_validation_error_is_not_warning_logged`. | Preserve error codes/details when refactoring; count-only assertions would miss contract regressions. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=N (request error translation is synchronous and has no cancellation or recovery lifecycle); P=N (request-scoped; no persisted user state); B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-XCT-005 | MCP typed operations and stable interface IDs (agent workspace contract) | `agent_mcp.py`, backend agent APIs | `test_services/test_agent_mcp.py::test_create_tool_and_workflow_nodes_send_typed_operations`, `::test_interface_tools_address_stable_ids_and_structural_scope`, `::test_connections_select_explicit_edge_variant`, `::test_missing_agent_state_is_machine_readable`; HTTP MockTransport captures operation payloads. | Add real MCP stdio client → backend → accepted draft → GUI update journey; mocked transport/registration does not certify installation or generated client configuration. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| RT-XCT-006 | Desktop startup identity, window configuration, orderly shutdown (v1 §§1,2.8) | `desktop.py`, entry point | `test_desktop.py` validates app icon/name, URL, host/port, dialogs, child editor window, cleanup ordering and stop timeout through mocks. | Packaged artifact launch, native event loop, code signing/install, macOS/Windows/Linux-specific behavior remain release/manual acceptance; headless tests must not claim these passed. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=N (startup shell has no portable saved state); B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |

## Priorities after the campaign resumes

Follow the [campaign plan](platform-test-plan.md) for global ordering: resolve the reproduced Run Selected issue and prove ordinary execution/results through the main GUI journey before expanding lifecycle and integration scenarios.
The gaps below remain mandatory final-coverage obligations; assign stable feature IDs and record applicable scenario, browser/deployment, and evidence status as they are completed.
1. Extend the verified held real sequential processing fixture from cancellation/reload/mutation-lock into actual failure, visible error/log attribution, correction, and successful rerun.
2. Extend the verified browser result journey from exact rows, filters, sorted source identity, full CSV content, and paging into merged/stacked views and durable column widths.
3. Verify a real two-run result bundle and GUI archive download, then cover a valid image in the browser viewer.
4. Complete pending Napari/deployment and MCP end-to-end audits, and keep native/external integrations in the manual acceptance checklist.

These are gaps or audit work items, not assumed platform defects.
Unexpected behavior with an unclear repair follows the [Sol → Astra → owner escalation protocol](platform-test-plan.md); preserve evidence before changing expectations or implementation.
