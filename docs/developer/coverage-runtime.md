---
orphan: true
---

# Runtime and integration coverage inventory

This is a partial inspected coverage inventory, not a passing certification report.
The audit baseline is `fcb4408`; no tests were executed during this inventory pass.
The campaign paused on an identity-generation issue reported by the workflow audit before the proposed runtime test changes began.
Selectors below are relative to `backend/tests/` unless prefixed with `frontend/`; use `scripts/test focus backend tests/<selector>` for backend cases.
File-level selectors identify a relevant family, while named selectors identify assertions inspected directly or a specific follow-up boundary.
Parallel scheduling, HPC, Parsl, managed remote execution, and distributed engines are excluded.
An in-process Direct computation and a mocked worker lifecycle are distinct from real sequential Wetlands worker certification.

## Execution, cache, and outputs

| Behavior and authority | Implementation owner | Existing evidence and negative boundary | Gap or next acceptance step |
| --- | --- | --- | --- |
| Run accepted draft; reject stale revision or unequal graph (v1 §2.4.5, v2 §§8–9) | `routers/execution.py`, execution manager, draft coordinator | Router selectors `test_run_selected_uses_accepted_draft_graph`, `test_run_rejects_stale_draft_before_execution_side_effects`, and `test_run_rejects_same_revision_graph_mismatch` bind accepted graphs and reject before manager start. Manager `selected_run` regressions cover full versus selected diagnostics, selected/upstream/unrelated workflow scopes, empty/unknown/unresolved/mixed targets, global errors, missing compilation, and dangling edges. Browser `execution.spec.ts`, “Run Selected executes a valid branch while an unrelated invalid branch remains unexecuted”, proves the exact request, valid upstream execution and rows, untouched unrelated node, absent unrelated cache/result, and complete accepted graph. | The selected valid-branch contract is verified in Chromium and Firefox at `8908388`; selection expansion through more complex recursive interfaces remains part of nested-workflow coverage. |
| Preserve identity across compilation and storage retries (v1 §2.4.5, v2 §12) | execution router/manager | `test_routers/test_execution_router.py::test_run_cannot_accept_same_id_replacement`, `::test_run_storage_retry_retains_one_pending_execution_identity`; explicit concurrent replacement/retry arrangements. | Include this family in per-case local selection even though neighboring execution-runtime tests cover remote features. |
| Prevent a second run, lock mutation during execution, and serialize ordinary mutations (v1 §2.4.5; v2 §7) | execution manager/router | `test_routers/test_execution_router.py::test_run_conflict_returns_409`, `::test_clear_while_running_returns_423`; `test_services/test_execution_manager.py::test_idle_mutation_leases_serialize_without_execution_conflict`, `::test_idle_mutation_lease_rejects_an_actual_execution`; `test_routers/test_workflows.py::test_save_waits_for_admitted_draft_validation_then_succeeds` holds real draft validation, proves Save waits, Run rejects, and Save succeeds after release. | Browser currently proves successful source execution and the formerly intermittent child-port Save path, not a visible mutation-lock attempt during a held execution. |
| Real Direct source execution (v2 §9) | graph builder/execution manager | `test_integration/test_real_execution.py`; `frontend/tests/e2e/execution.spec.ts` creates a workflow in GUI, sets source via API while unmounted, runs via GUI, checks backend completion and executed class; `canvas-interactions.spec.ts`, “builds, runs, inspects, saves, and reopens a real workflow through the GUI” creates and connects real `SeedNumbers` and `IncrementNumbers` nodes only through controls, runs the accepted graph, and checks exact visible merged rows. | The main browser journey certifies ordinary Direct GUI execution, not worker process isolation; real sequential Wetlands evidence remains separate below. |
| Recursive Direct execution and scoped result attribution (v2 §§5,7,9,13) | recursive graph compiler, execution manager, result projection | Browser `nested-execution.spec.ts`, “executes nested Direct work and preserves scoped results after save and reopen”, runs a real root→nested→root DataFrame chain through the GUI, checks `nested_step/nested_increment` status and projection identity, exact nested rows `2,3,4`, exact downstream rows `(2,3),(3,4),(4,5)`, durable results after Save/reload/reopen, and sibling isolation. | Verified in Chromium and Firefox at integrated `fd96f8f`; this DataFrame-only Direct journey does not certify worker isolation, nested cancellation/failure, or recursive owned-source execution. |
| Real sequential worker isolation and files (library §5, v2 §9) | execution/compiler, Wetlands public API | `test_integration/test_platform_fixture_contracts.py::test_processing_fixture_runs_in_real_sequential_wetlands_worker` proves distinct PID, transformed values, and output files. `test_integration/test_held_worker_fixture.py` validates the cancellation fixture's exact worker origin, sequential Wetlands graph, cooperative acknowledgement, and absent output. | External opt-in environment creation remains separate from the dev-only browser fixture; do not replace either claim with a DataFrame-only graph. |
| Progress and completion retain one context (v1 §2.4.5) | execution manager, WebSocket bus | `test_services/test_execution_manager.py`, using named methods such as `test_one_context_is_bound_to_progress_completion_and_terminal_status`; bodies simulate progress from controlled workflows and assert execution/workflow/revision. | Prefer collected exact node IDs in final manifest; mocked progress is not a real worker transport check. |
| Failure, retry, and stop retain completed output (v1 §2.4.5; library §§9,13) | execution manager | Manager regressions cover cancellation preservation and retry identity. Browser `execution-cancellation.spec.ts` uses socket state rather than sleeps to hold a real sequential worker, verifies Stop reaches cooperative cancellation before idle, retains exact completed-source rows, exposes no partial worker result, preserves workflow/revision/run identity across reload, and reruns to exact values in a distinct worker process. | Cancellation is verified in Chromium and Firefox at integrated `da2ced2` with BioImageFlow 0.7.2. Add the corresponding actual worker failure, visible diagnostic/log, correction, and rerun journey; broader retry variants remain lower-level. |
| Cache invalidation propagates downstream, selected clears take priority (v1 §2.4.5; library §6) | `services/execution.py::clear_node_cache`, output-view service, NodePanel | `test_services/test_cache_clearer.py::test_clear_propagates_out_of_date_to_downstream`, `::test_cleared_node_takes_priority_over_out_of_date`, `::test_invalid_graph_does_not_clear_existing_cache`; `::test_clear_multiple_nodes_shared_downstream` builds a valid two-input consumer and asserts both cleared sources are `unexecuted` while their shared descendant is `out_of_date`. `test_output_views_clear.py` verifies scoped latest pointer/materialization removal with a surviving sibling. Browser `everyday-node-editing.spec.ts` proves root bulk Clear confirmation/cancel, exact graph/revision preservation, selected output removal across reload, accepted-draft gating, zero clear requests on persistence failure, and retained results after refusal. | Root selected-node Clear is verified in Chromium and Firefox at integrated `30faeb6`; downstream stale-result presentation and nested-canvas scoped clearing remain later result/recursive boundaries. |
| Validation cache flags agree with actual computation (library §6) | graph validator/builder | `test_services/test_cache_parity.py` executes explicitly sequential Direct DataFrame tools and validates before/after seed and parameter edits. | This is in-process Direct cache evidence, not worker serialization or isolation. |
| Output template chooses file name and changes signature (library §7; v1 §3.5) | library translation and output template UI | `test_services/test_library_output_templates.py::test_workflow_from_dict_uses_node_output_template`, `::test_output_template_changes_execution_signature`; the explicitly sequential Direct fixture writes a real asset and checks a distinct cache record directory. | Browser template editing plus actual resulting filename is missing; no worker acceptance is claimed. |
| Latest projection chooses symlinks or pointer fallback (v2 §12; library §7) | `services/output_views.py` | `test_services/test_output_views.py::test_auto_uses_symlinks_when_supported`, `::test_auto_falls_back_to_pointer_without_copying`, `::test_runtime_symlink_failure_falls_back_to_pointer`; capability and export calls mocked. | Real platform-specific filesystem capability belongs in release acceptance; do not claim mocked symlink denial proves Windows behavior. |
| Read exact latest record and reject incomplete data (v1 §2.4.4; library §6) | `services/result_store.py` | `test_services/test_result_store.py::test_get_latest_dataframe_loads_library_generated_v1_view`, `::test_get_latest_dataframe_raises_not_ready_for_zero_byte_parquet`, `::test_zero_byte_latest_hash_does_not_fall_back_to_stale_data`, `::test_malformed_node_id_does_not_probe_outside_latest`. | Audit legacy-link fallback separately before removal; existence of a legacy-named test alone does not authorize deleting currently specified library read behavior. |
| Latest-results export copies assets and rolls back failed replacement (v2 §12) | `services/workflow_exports.py` | `test_services/test_workflow_exports.py::test_real_folder_export_materializes_owned_asset_and_provenance_as_copies`, `::test_install_failure_rolls_back_previous_export`, `::test_folder_export_rejects_symlink_parent_and_target`; real filesystem alongside mocked export boundaries. | Add GUI download and inspect archive bytes; a click/download event alone is insufficient. |
| Workflow-results bundle pins one successful run (v2 §12) | workflow exports/router | `test_services/test_workflow_exports.py::test_bundle_pins_latest_success_once_and_writes_manifest` asserts one run lookup, exact run-scoped export args, manifest hash; `::test_bundle_without_successful_run_is_unavailable`; `test_routers/test_workflows.py::test_import_rejects_renamed_results_bundle_before_archive_adapter`. | Current pinning test mocks storage lookup/export. Add real two-run execution plus bundle readback proving all nodes belong to the pinned run instead of mixed latest outputs. |

## Tables, datasets, images, and viewers

| Behavior and authority | Implementation owner | Existing evidence and negative boundary | Gap or next acceptance step |
| --- | --- | --- | --- |
| Single-node pagination, filtering, sorting, CSV keep source row identity (v1 §2.4.4, v2 §13) | nodes/data-table routers, result and projection stores | Backend node and projection selectors prove filter-before-sort-before-page, exact source positions, and matching query/CSV behavior. Browser `canvas-interactions.spec.ts`, “filters, sorts, pages, and exports an exact real execution result”, creates and runs a real deterministic 60-row Direct source through the GUI, applies a visible `label starts with keep-` filter, sorts `score` descending, checks all correlated visible source-row/label/score values against exact `source_rows`, changes page size from 25 to 50, uses next and direct-page navigation with exact requests/ranges, and parses the complete downloaded merged CSV header and all 30 ordered rows. | The primary single-selection merged-table journey is verified in Chromium and Firefox at integrated `d1072ce`. Image action after sorting, multi-selection merged/stacked fallback, and durable column-width behavior remain separate obligations. |
| Merge tables only when lineage preserves selected rows (v2 §13) | `services/data_table_projection.py` | `test_services/test_data_table_projection.py::test_exact_indices_merge_and_snapshot_records_before_loading` asserts all record pointers captured before loads; `::test_parent_values_expand_to_finest_selected_lineage`; `::test_filtered_selected_anchor_falls_back_instead_of_losing_rows`; duplicate/divergent-index cases. | Real GUI multi-selection merged/stacked fallback and source navigation missing from inspected execution journey. |
| Column labels and filters precede sorting/paging (v2 §13) | projection service/data-table router | `test_services/test_data_table_projection.py::test_colliding_labels_are_qualified_and_sort_happens_before_paging`, `::test_filters_apply_before_sorting_and_paging_and_report_both_totals`; `test_routers/test_data_table.py::test_query_and_csv_apply_the_same_filters`. | Column-resize persistence/reset browser journey needed; current execution journey checks handle geometry only. |
| Upload partial success, size cap, filename and path boundary (v1 §2.4.10) | dataset router/store | `test_routers/test_datasets.py::test_upload_partial_success`, `::test_upload_oversize_mid_stream`, `::test_upload_dos_guard_rejects_huge_content_length`, `::test_upload_multi_file_total_over_per_file_cap`; store cleanup and sanitization tests. | Browser upload uses fake TIFF bytes and certifies file management only, never image readability. Add browser per-file failure plus successful sibling visibility. |
| Dataset folder operations preserve physical path and reject stale destructive preview (v2 §16) | dataset store/router/panel | `test_services/test_dataset_store.py::test_folder_rename_and_move_do_not_change_dataset_path`, `::test_folder_move_rejects_descendant_cycle`, `::test_recursive_delete_requires_current_preview_revision`; `frontend/tests/e2e/datasets-panel.spec.ts` checks upload, rename, drag creation, grouped moves and checkbox hierarchy. | Existing browser title wait is generic; use intended workflow identity when extending. Add reopen plus Files parameter snapshot assertions and confirmed delete consequences. |
| Cancel browser upload (v1 §2.4.10, v2 §16) | dataset upload client/panel | `frontend/tests/e2e/datasets-panel.spec.ts` “cancels a file dropped on the canvas while its upload is active” holds intercepted request, clicks Cancel, checks cancelled message and Retry. | Interception proves client UI cancellation, not server partial-file cleanup; store tests own cleanup independently. Retry round-trip is not asserted. |
| Image response identity, OME-TIFF conversion, offset JSON (v1 §2.4.8) | nodes router | `test_routers/test_nodes.py::test_node_image_endpoint_can_convert_png_to_ome_tiff_for_avivator` writes real 4×4 PNG and reads TIFF pixels; `::test_node_image_endpoint_returns_offsets_json_for_avivator`; unreadable-image 422 and stale conversion-cache prune tests. | Plain serving tests use fake TIFF bytes; real browser Avivator load/render requires valid fixture and network/renderer inspection. Multidimensional conversion boundaries need a dedicated audit beyond the inspected 2D case. |
| Thumbnail placeholder, relative asset path, generator formats (v1 §2.6) | thumbnail manager/generator, nodes router | `test_routers/test_nodes.py::test_thumbnail_endpoint_signals_pending_for_placeholder`, `::test_thumbnail_endpoint_resolves_record_relative_asset_paths`; `test_services/test_thumbnail_generator.py` covers constant arrays, RGBA and canonical extension links with mocked reader boundaries. | Real thumbnail environment startup and appearance in GUI are external/integration acceptance, not certified by reader mocks. |
| Reveal and native dialogs (v1 §§2.4.7,3.13) | filesystem router, desktop bridge | `test_routers/test_filesystem.py::test_reveal_path_calls_reveal_function`, `::test_reveal_path_handles_os_error`; `test_desktop.py` mocks selection/cancel/drop paths and process launches. | Native OS dialog, physical drop, file browser launch require manual desktop acceptance. Deployment denial coverage is owned by deployment-mode tests, not this router file alone. |
| Fiji resolves workflow image and launches separate arguments (v1 §2.4.8) | Fiji router/launcher | `test_services/test_fiji_launcher.py::test_open_launches_image_as_separate_argument` captures exact argv with spaces; absent config/invalid launcher/unsupported CPU/process failure cases; `test_routers/test_fiji_router.py` owns mode and result-address boundaries. | Launchers are fake executable files and process callable is replaced. Actual Fiji install and displayed image require manual acceptance. |
| Napari environment and reconnect lifecycle (v1 §2.4.8) | Napari manager/launcher/router | `test_services/test_napari_manager.py`, `test_services/test_napari_launcher.py`, `test_routers/test_napari_router.py` are existing focused families; exact assertion-level review remains pending. | Do not mark complete until bodies and mode gates are fully audited; actual Qt window/layers remain external/manual. |

## Events, diagnostics, agents, and desktop

| Behavior and authority | Implementation owner | Existing evidence and negative boundary | Gap or next acceptance step |
| --- | --- | --- | --- |
| WebSocket subscriptions, reconnect snapshot, slow-consumer isolation (v1 §2.5) | `ws/handler.py`, app wiring | `test_ws/test_handler.py::test_execution_messages_and_snapshot_share_one_context`, `::test_endpoint_sends_initial_status_snapshot_when_provider_configured`, `::test_queue_backpressure_drops_oldest`, `::test_slow_consumer_does_not_block_fast_consumer`; endpoint and queue tests. | Add browser disconnect/reconnect during held sequential execution and prove restored state belongs to originating workflow. Exclude only remote snapshot test by exact identity. |
| Execution logs carry provenance; background logs stay global | logging bridge/handler | `test_ws/test_logging_bridge.py::test_record_provenance_cannot_be_relabelled_by_a_later_execution`, `::test_unattributed_worker_record_is_not_claimed_by_a_later_execution`; `test_ws/test_handler.py::test_broadcast_log_preserves_execution_context_and_global_logs`. | v2 §1.8 and implementation commit `a8d9533` establish contextual execution logs; ISSUE-002 reconciled the stale v1 §2.5 table wording. |
| One contextual error in history and Logger, readable traceback (v1 §§3.7,3.11) | frontend logging/error stores and panels | `frontend/tests/e2e/error-handling.spec.ts` “contextual execution failure is recorded once in history and logger” injects synthetic failure via E2E endpoint, asserts log before completion, context, one unread error and one log entry. Other cases test read toggles and node navigation. | This is real event transport/UI projection but not a failing real tool. Add actual sequential tool failure journey after worker fixture exists. |
| Structured HTTP error details and expected-error logging (v1 §2.4.1b) | error handler | `test_routers/test_error_handler.py::test_http_exception_preserves_declared_structured_details`, `::test_expected_forbidden_http_exception_is_not_warning_logged`, `::test_request_validation_error_is_not_warning_logged`. | Preserve error codes/details when refactoring; count-only assertions would miss contract regressions. |
| MCP typed operations and stable interface IDs (agent workspace contract) | `agent_mcp.py`, backend agent APIs | `test_services/test_agent_mcp.py::test_create_tool_and_workflow_nodes_send_typed_operations`, `::test_interface_tools_address_stable_ids_and_structural_scope`, `::test_connections_select_explicit_edge_variant`, `::test_missing_agent_state_is_machine_readable`; HTTP MockTransport captures operation payloads. | Add real MCP stdio client → backend → accepted draft → GUI update journey; mocked transport/registration does not certify installation or generated client configuration. |
| Desktop startup identity, window configuration, orderly shutdown (v1 §§1,2.8) | `desktop.py`, entry point | `test_desktop.py` validates app icon/name, URL, host/port, dialogs, child editor window, cleanup ordering and stop timeout through mocks. | Packaged artifact launch, native event loop, code signing/install, macOS/Windows/Linux-specific behavior remain release/manual acceptance; headless tests must not claim these passed. |

## Priorities after the campaign resumes

Follow the [campaign plan](platform-test-plan.md) for global ordering: resolve the reproduced Run Selected issue and prove ordinary execution/results through the main GUI journey before expanding lifecycle and integration scenarios.
The gaps below remain mandatory final-coverage obligations; assign stable feature IDs and record applicable scenario, browser/deployment, and evidence status as they are completed.
1. Extend the verified held real sequential processing fixture from cancellation/reload/mutation-lock into actual failure, visible error/log attribution, correction, and successful rerun.
2. Extend the verified browser result journey from exact rows, filters, sorted source identity, full CSV content, and paging into merged/stacked views and durable column widths.
3. Verify a real two-run result bundle and GUI archive download, then cover a valid image in the browser viewer.
4. Complete pending Napari/deployment and MCP end-to-end audits, and keep native/external integrations in the manual acceptance checklist.

These are gaps or audit work items, not assumed platform defects.
Unexpected behavior with an unclear repair follows the [Sol → Astra → owner escalation protocol](platform-test-plan.md); preserve evidence before changing expectations or implementation.
