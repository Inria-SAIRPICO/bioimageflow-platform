---
orphan: true
---

# GUI editing and authoring coverage inventory

This partial inventory records inspected assertions, not a claim that every listed test passed in the current campaign.
Rows combine historical inspection with subsequent campaign repairs; use each row's evidence and the current checkpoint to distinguish completed work from remaining gaps.
The [campaign plan](platform-test-plan.md) owns priority order and the systematic per-feature completion requirements; all in-scope gaps remain obligations after the main GUI paths.
Every row declares its stable feature ID directly; that ID is the sole mapping key to the [obligation dashboard](platform-test-dashboard.md), while labels and row positions are descriptive only.
The `Scenario contracts` cell records all five dashboard dimensions with the dashboard state: applicable cells delegate to the row's named behavior and evidence/gap prose, while each `N` repeats its concise non-applicability reason.
It covers local GUI editing and authoring; workflow lifecycle, recursive interfaces, results, integrations, and execution have complementary campaign inventories.
HPC, Parsl, distributed engines, remote execution, and parallel execution behavior are excluded.
Browser automation remains an opt-in lane called after substantial changes, using the authoritative commands in [Testing](../testing.md).

In the tables, browser paths are relative to `frontend/tests/e2e`, frontend paths to `frontend/src`, and backend paths to `backend/tests`.
Quoted titles are exact test selectors usable with `scripts/test focus e2e <file> --grep '<title>'` or `scripts/test focus unit <file> -t '<title>'`.
Backend method names are searchable selectors usable with `scripts/test focus backend <file> -k '<name>'`.
An API-seeded browser fixture proves the browser behavior asserted after setup; it does not prove the equivalent setup action works through the GUI.

## Shell, settings, and restoration

| Stable ID | Behavior and authority | Implementation | Existing assertion evidence | Missing or bounded evidence | Scenario contracts |
| --- | --- | --- | --- | --- | --- |
| GUI-SHL-001 | Default docks and menu, v1 §3.2 | `App.vue`, `components/layout/AppShell.vue` | Browser `app-shell.spec.ts`: `renders Dockview layout with menu bar and panels`; `View menu toggles panel visibility` | Dock resize, drag/reorder, close/reopen, and layout persistence need a browser journey. | S=G: exercise the named successful behavior and assert the exact row outcome; R=N (no invalid-input or refused-action contract for shell layout); F=N (no cancellation or recovery lifecycle for static shell layout); P=G: reload or reopen durable state and assert the row's specified identity and content; B=N (no identity, concurrency, ownership, or permission boundary for shell layout) |
| GUI-SHL-002 | Empty workspace never creates a special workflow, v1 §4.3 | `services/startupWorkflow.ts`, `App.vue` | Browser `workflow-creation.spec.ts`: `shows a non-persistent chooser when no workflow exists`; unit `services/__tests__/startupWorkflow.test.ts`: `returns null instead of creating a special startup workflow when none can load` | Real GUI verifies no workflow POST or list mutation, disabled empty Open, lossless Open/New cancellation, exactly one explicit creation with generated identity, and same-identity reload in Chromium and Firefox at `75be9c2`. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-SHL-003 | Recovery preference and obsolete identities, v1 §4.3; v2 §12 | `services/startupWorkflow.ts`, recovery persistence coordinator | Unit startup tests prove a newer fallback wins and obsolete or older recovery is cleared. Browser `critical-operation-races.spec.ts`, “recovers an accepted root draft and makes Cancel, conflict, and Discard lossless”, performs an actual parameter edit on a valid connected graph, removes the fallback, and proves the exact accepted backend draft identity, graph, edge, binding, revision, validation, and dirty state across reload and sibling switching. Browser `workflow-creation.spec.ts` additionally proves an obsolete last-opened preference is cleared when the workspace is empty. | Accepted backend recovery, obsolete-preference cleanup, and focused fallback ordering are verified; a single browser journey that creates a newer fallback through a real interrupted write and packaged crash/restart remain separate boundaries. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-SHL-004 | Recovered edges have real geometry, v1 §3.3, §4.3 | `components/canvas/CanvasView.vue` | Browser `graph-persistence.spec.ts`: `auto-saved graph restores nodes and edges after reload` verifies seeded SeedNumbers/IncrementNumbers metadata, compatible whole-DataFrame wiring, backend validation, exact recovered identity and edge geometry. The root recovery journey separately proves the visible edge and exact accepted backend draft after a GUI edit and reload. | The IndexedDB fixture remains intentionally controlled; the ordinary main GUI journey separately covers user edit, Save, switch, and exact reopen. | S=V: exercise the named successful behavior and assert the exact row outcome; R=N (geometry rendering accepts no user input to refuse); F=N (geometry rendering has no cancellation or recovery lifecycle); P=V: reload or reopen durable state and assert the row's specified identity and content; B=N (geometry is derived within the owning canvas and has no separate boundary) |
| GUI-SHL-005 | Preferences loading and desktop availability, v1 §3.12 | `components/panels/SettingsPanel.vue`, settings store | Browser `settings.spec.ts`: `opens via Edit > Preferences... and shows the settings tabs`; `workflow-creation.spec.ts`: `desktop shell shows Create Tool even when settings are unavailable` | Add settings load-error retry and close/reopen behavior. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-SHL-006 | Persisted page-size choice, v1 §3.12.4 | Display settings section, settings store | Browser `settings.spec.ts`: `Display exposes the persisted Node Data page-size preference`; backend `test_routers/test_settings.py`: `test_patch_node_data_page_size` | Browser currently reads 250 only; change selection, verify PATCH, reload, and inspect a newly opened table. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=N (page-size selection has no cancellation or recovery lifecycle); P=G: reload or reopen durable state and assert the row's specified identity and content; B=N (the global display preference has no scoped identity or ownership boundary) |
| GUI-SHL-007 | OMERO cards and secret handling, v1 §3.12.6; v2 §14.1 | OMERO settings section, settings router/store | Browser `settings.spec.ts`: `OMERO instance cards keep fields and actions visible without horizontal scrolling`; backend `test_routers/test_settings.py`: `test_patch_omero_password_persists_metadata_only`, `test_patch_omero_duplicate_names_return_422`, `test_patch_omero_keyring_failure_returns_error_without_persisting` | Responsive rendering is covered; Add/Save/Duplicate/Remove and failure feedback need browser coverage with a controlled keyring boundary. Actual OS credential-store integration remains manual. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-SHL-008 | Desktop/editor settings and forbidden webapp mutation, v1 §2.4.6, §3.12 | settings models/router, editor/image-viewer sections | Backend `test_routers/test_settings.py`: `test_patch_unsafe_webapp_features_rejected`, `test_webapp_fiji_configuration_is_rejected`; browser Fiji settings explanation test | Native selection and actual external application launch remain manual. See integration inventory for viewers. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |

## Canvas editing and parameters

| Stable ID | Behavior and authority | Implementation | Existing assertion evidence | Missing or bounded evidence | Scenario contracts |
| --- | --- | --- | --- | --- | --- |
| GUI-EDT-001 | Inspect/add/select node from real catalog, v1 §3.3.1, §3.4; v2 §15 | ToolsPanel, active canvas commands, CanvasView | Browser `canvas-interactions.spec.ts` asserts the real seeded `SeedNumbers` package/schema: single-click opens and closes exact documentation without mutation, double-click preserves the empty-canvas viewport and persists one valid interactive node, and drag persists one node at the requested position. A failed draft PUT after real double-click leaves one local node, unchanged accepted graph/revision, and visible Retry; retry accepts that same node exactly once, and reload preserves it. Unit coverage distinguishes both row gestures, isolates secondary actions, routes creation only to the active root/nested canvas, and rejects missing or disposed ownership. | ISSUE-003 first repaired the unhandled creation event; the owner subsequently assigned inspection to single-click and creation to double-click. Additional node naming/collision cases belong to their dedicated rows. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-EDT-002 | Fit large/small graphs and encoded paths, v1 §3.3.4 | CanvasView | Browser `canvas-viewport.spec.ts`: `centers a small workflow with a working folder grid @critical`, corresponding `large` case; checks bounds, centering, grid references, Run Selected visibility | User zoom/pan, 5% minimum, fit shortcut, first-node viewport preservation, and resize behavior remain gaps. | S=G: exercise the named successful behavior and assert the exact row outcome; R=N (viewport fitting accepts no invalid domain input); F=N (viewport fitting has no cancellation or recovery lifecycle); P=N (fit geometry is recomputed and is not durable workflow state); B=N (viewport fitting has no identity, concurrency, ownership, or permission boundary) |
| GUI-EDT-003 | Whole-DataFrame edges and dynamic columns, v1 §3.3.1–3; library §3.5–3.6 | pins, edge codec, resolved-output store | Browser `canvas-interactions.spec.ts`: `new dynamic tools connect cleanly and expose their resolved columns` verifies Generate has no parameter pins, whole-DataFrame edge, accepted graph, exact execution rows. Browser `everyday-node-editing.spec.ts`, “refuses a column-to-DataFrame connection without replacing the accepted edge”, uses a real pointer drag and verifies no draft revision or graph change, exact surviving edge, and reload identity. Its pointer disconnect/reconnect recovery journey forces one draft PUT failure on each operation, proves unchanged accepted graph/revision while local edge state remains editable, retries through the visible control, then verifies exact edge identity, Undo/Redo, and reload. | Incompatible cross-region refusal and whole-DataFrame connection recovery are verified in Chromium and Firefox through `bd92dfc`; positional auto-grow/compaction with multiple upstreams remains a separate variant. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: incompatible pointer connection leaves the accepted graph and revision unchanged; F=V: failed pointer disconnect/reconnect writes retain accepted state and visible Retry restores the intended edge state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: preserve existing edge/endpoint identity across refusal and reload |
| GUI-EDT-004 | Column input labeling and disconnect, v1 §3.3.3 | InputPin and CanvasView | Unit `components/canvas/__tests__/InputPin.test.ts` verifies labels, reserved published inputs, and edge removal on connected-handle grab. Browser `everyday-node-editing.spec.ts` disconnects and reconnects the DataFrame edge through pointer gestures, then refuses an incompatible column-to-DataFrame drag without replacing that edge or advancing the draft revision. Its separate real connected-column-input journey proves the exact source-derived label and edge, forces a failed pointer disconnect draft PUT, verifies unchanged accepted graph/revision and local disconnected label, then retries, Undo restores the exact column edge, Redo removes it, and reload retains the accepted disconnected state. | Column-input failure/retry and label recovery are verified in Chromium and Firefox through `bd92dfc`; mapped/collective column rewiring remains an unexercised variant. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: incompatible cross-region drag leaves the accepted edge and draft unchanged; F=V: failed connected-column disconnect keeps durable edge/revision intact and visible Retry recovers; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-EDT-005 | Mapped/collective column appearance, v1 §3.3.2 | edge components and tool metadata | Unit `components/canvas/__tests__/edges.test.ts` | Browser geometry/reactive metadata update evidence is missing; row-consumption presentation is in scope, parallel execution is not. | S=G: exercise the named successful behavior and assert the exact row outcome; R=N (row-consumption appearance accepts no user input to refuse); F=N (derived edge appearance has no cancellation or recovery lifecycle); P=N (appearance is derived from metadata and is not persisted); B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-EDT-006 | Parameters immediately feed accepted Run, v1 §2.4.3, §3.5.3 | NodePanel, canvas commands, persistence coordinator | Browser `critical-operation-races.spec.ts`: `immediate Node Panel parameter edit is the exact accepted Run input`, `delayed parameter persistence keeps node presentation stable and success quiet`; `canvas-interactions.spec.ts`, “builds, runs, inspects, saves, and reopens a real workflow through the GUI” edits the real `IncrementNumbers.number` numeric control and proves accepted, executed, saved, and reopened values. Browser `parameter-controls.spec.ts`, “typed parameter controls refuse invalid lists, recover, reset, connect, and persist”, exercises a real introspected ProcessingTool through Literal enum, boolean, JSON list error/refusal/recovery, reset-to-default, pin reveal, column connection, connected-field read-only presentation, accepted validation, Save, workflow switch, and exact reopen identity. A focused NodePanel unit verifies canonical encoded-handle lookup for connected inputs. | The representative numeric and typed-control matrix is verified in Chromium and Firefox through integrated `1261326`; the immediate-edit race case still uses an intercepted Run, while the main journey separately proves real numeric execution. File/resource/output-template controls retain their dedicated rows and obligations. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-EDT-007 | Node display-name rename preserves ID, v1 §3.5.1 | NodePanel | Browser `everyday-node-editing.spec.ts`, “renames without changing node or edge identity and refuses a duplicate name”, enters a Unicode display name, verifies stable node IDs and edge identity/endpoints in the accepted draft and after reload, then proves a duplicate-name attempt displays an inline error without advancing the accepted revision. | Normal, duplicate, identity, and persistence behavior are verified in Chromium and Firefox at `ec0ca3b`; empty-name and execution-lock rename feedback remain lower-level or future browser obligations. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=N (rename validation is synchronous; refusal is owned by R and no separate recovery follows); P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-EDT-008 | Select, multiselect, collapse, enable/disable, v1 §3.3.1, §3.5 | CanvasView, ToolNode, NodePanel | Browser `everyday-node-editing.spec.ts` proves click selection, Shift-toggle multiselection, empty-click deselection, Shift-drag box selection, bulk disable/enable with accepted-draft reload, header collapse persistence, and the single-node enabled toggle. The batches repaired missing bulk actions and a non-forwarded collapse event; later atomic command coverage proves one bulk click is one history/persistence transition. Its enabled “Enable all” action on an already-enabled multiselection proves zero draft PUTs, unchanged accepted graph/revision, and no Undo history entry before normal bulk disable/Undo/Redo proceeds. | Primary root-canvas selection, box selection, collapse, enablement, and redundant-action refusal are verified in Chromium and Firefox through `1d7cbd7`; nested-canvas destructive behavior retains separate recursive ownership boundaries. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: an enabled redundant bulk action is a no-op with no draft write or history entry; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-EDT-009 | Bulk node deletion and output clearing, v1 §3.5, §4.6 | CanvasView, NodePanel, canvas commands, execution clear | Browser `everyday-node-editing.spec.ts`, “box-selects and atomically deletes nodes with their incident edge”, uses real Shift-drag selection, proves cancel is an exact no-op, confirms one accepted deletion of both nodes and the incident edge, then verifies one-step Undo/Redo, valid accepted state, reload, and inactive-workflow isolation. “clears selected outputs only after confirmation without mutating the graph” runs real computation, proves Cancel retains the result and graph/revision, confirms Clear makes the selected output unavailable across reload without graph mutation, and uses explicit root-only command capability. “refuses to clear outputs when the pending draft cannot be accepted” forces draft persistence failure and proves zero clear requests and retained outputs. Focused units cover active-canvas identity capture, nested Clear hiding, and execution-lock-disabled controls. | The primary root bulk Delete/Clear paths are verified in Chromium and Firefox at integrated `30faeb6`; plain drag remains pan and Shift-drag is the implemented box-selection gesture. Nested-canvas result clearing is intentionally not exposed until a scoped recursive result-ownership path is implemented. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-EDT-010 | Undo/redo per action and isolated baseline, v1 §4.6 | `composables/useUndoRedo.ts`, CanvasView, UI store, MenuBar | Unit tests cover stack round-trip/baseline/redo clearing, bulk command routing, no-op parameter rejection, and per-canvas availability. Browser `everyday-node-editing.spec.ts` proves one-step bulk enablement Undo/Redo, then action-by-action add-node, disconnect-edge, and numeric-parameter Undo/Redo through accepted drafts and reload. Its workflow-switch journey closes/reopens exact workflows, observes disabled Undo directly, and proves shortcuts cannot mutate either accepted graph. Browser `workflow-interface.spec.ts` additionally proves nested Ctrl+Z/Redo advance only the private session revision while the accepted root graph/revision remain unchanged; after switching tabs, root Undo changes only the root and leaves the private snapshot exact. | Core mixed edits, parameter granularity, redo, bulk atomicity, root-switch isolation, and nested/root history ownership are verified in Chromium and Firefox through `4ec5dbc`; destructive bulk variants remain separate feature boundaries. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: nested and root histories mutate only their owning graph/revision after tab switches |
| GUI-EDT-011 | Clipboard preserves internal structure and changes identities, v1 §3.3.1; v2 §6.3 | `utils/clipboard.ts`, CanvasView | Unit `utils/__tests__/clipboard.test.ts`: `copies and pastes a complete workflow node while changing only parent identities`, `rejects records without the current strict discriminator envelope`. Browser `clipboard-shortcuts.spec.ts`, “copies a connected selection, pastes only its internal structure, and refuses malformed clipboard data”, copies an actual click/Shift-click selection, verifies fresh node and edge identities with only the internal edge, reloads the accepted graph, and proves malformed clipboard refusal leaves its revision and graph exact. | The primary journey is verified in Chromium and Firefox at integrated `790345a`; native clipboard permission is controlled because Firefox Playwright cannot grant clipboard-read. Cross-workflow and recursive-source copy obligations remain in the workflow inventory. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-EDT-012 | Keyboard commands respect active context and text entry, v1 §6; v2 §15 | CanvasView, canvas command routing | Unit `composables/__tests__/canvasCommands.test.ts`: `routes Save to the active nested canvas command`, `routes a shared node id only to the explicitly active fixed canvas`, `rejects late calls through a disposed fixed-canvas resource`; `utils/__tests__/keyboard.test.ts` covers native and semantic text-entry targets. Browser `clipboard-shortcuts.spec.ts`, “routes core shortcuts to the active canvas and preserves text-field input”, exercises Delete/Backspace, select-all, copy/paste, Save, forced validation, fit, Escape, and tool-search focus on the active root canvas while proving an inactive workflow and focused rename field do not mutate. Browser `workflow-interface.spec.ts` proves Ctrl+S in the active nested tab applies its exact private graph to the root draft without promoting the saved workflow; after switching to the root tab, Ctrl+S saves that accepted root graph. | Root active-canvas and input-focus behavior plus nested/root shortcut routing are verified in Chromium and Firefox through `4ec5dbc`. | S=V: exercise the named successful behavior and assert the exact row outcome; R=V: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=V: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: active nested/root shortcuts affect only the owning persistence context |
| GUI-EDT-013 | Processing resources and output templates, v1 §3.5.4 | NodeResourcesTab, output-template controls | Unit `components/panels/__tests__/NodeResourcesTab.test.ts`: `shows declared, override, and effective resources and emits reset`, `emits the strict graph resource field names and capacity grammar`; `utils/__tests__/outputTemplates.test.ts` | Add browser CPU/memory field persistence/reset and template validation. Exclude concurrency allocation and parallel scheduler behavior. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |

## Tool catalog, authoring, and editors

| Stable ID | Behavior and authority | Implementation | Existing assertion evidence | Missing or bounded evidence | Scenario contracts |
| --- | --- | --- | --- | --- | --- |
| GUI-AUT-001 | Search and selected-tool reveal, v1 §3.4 | ToolsPanel | Browser `workflow-creation.spec.ts`: `search filters tool list to empty on nonsense query`; unit `components/panels/__tests__/ToolsPanel.test.ts`: `reveals a selected node tool through collapsed categories and search, then clears its highlight` | Add browser positive fuzzy query by tags/category and selected-row scrolling; nonsense query alone does not prove fuzzy matching. | S=G: exercise the named successful behavior and assert the exact row outcome; R=N (search has no invalid domain input; an unmatched query is successful empty-result behavior); F=N (search and reveal have no cancellation or recovery lifecycle); P=N (query and reveal state are not specified as durable); B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-AUT-002 | Create/custom rename/delete and editor opening, v1 §3.4 | ToolsPanel, CreateToolDialog, custom-tool service | Browser `hot-reload.spec.ts`: `custom DataFrame tool display name refreshes after an editor save` creates via GUI; unit CreateToolDialog invalid-name and POST cases; ToolsPanel custom action tests | Browser ProcessingTool creation, duplicate-name rejection, rename usage propagation, delete confirmation/cancel and blocked deletion remain gaps. Editor open is intercepted in current browser creation test. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-AUT-003 | Package versions/source install and errors, v1 §2.3, §3.4 | ToolsPanel, package services | Unit ToolsPanel tests cover URL/ZIP install, busy state, failed installed version, confirmation, uninstall, webapp restrictions | No dedicated package-management browser journey found. Use deterministic local package archives for UI operations; real published-package installation remains external certification. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-AUT-004 | Editable source watcher and code-only change, v1 §2.7 | tool_hot_reload service, WebSocket, registry store | Browser `hot-reload.spec.ts`: both tests exercise filesystem→backend→WebSocket→canvas and the real watcher test now fails explicitly if Playwright does not provision its isolated mutable fixture; backend `test_services/test_tool_hot_reload.py`: `test_polling_recovers_missed_custom_tool_save`, `test_custom_tool_reload_failure_preserves_previous_metadata`, `test_atomic_custom_tool_save_reloads_from_move_destination` | Browser invalid edit→visible error→valid edit recovery is missing. | S=V: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=V: reload or reopen durable state and assert the row's specified identity and content; B=V: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-AUT-005 | Embedded editor startup/progress and stable iframe, v1 §2.4.9 | CodeEditorPanel, editor service/workspace | Unit `components/panels/__tests__/CodeEditorPanel.test.ts`: `shows extension progress, elapsed time, and opens the Logger panel`, `keeps the existing iframe mounted while opening another file`, `ignores a load event from a previous iframe target`; backend `test_services/test_editor.py`: `test_cold_tool_open_returns_workspace_before_opener_activation`, `test_plain_status_does_not_generate_managed_workspace`, `test_editor_reuses_extensions_across_launches_and_repairs_missing_or_changed_content` | No complete browser editor journey found. Controlled status/opener fixtures can prove UI; real code-server installation, terminal continuity, package read-only workspace, detached/native windows need explicit external/manual acceptance. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-AUT-006 | Node-addressed source opening, v2 §14 | NodePanel, node-editor router | Frontend `api/__tests__/editor.test.ts`; backend `test_routers/test_node_editor.py` | Browser open bound source with a same-named catalog tool and nested-session source fork is missing; do not substitute catalog opening for node opening. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=N (opening or focusing a source is ephemeral; source persistence belongs to WF-SRC rows); B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |
| GUI-AUT-007 | Trusted Python authoring, v2 §11 | workflow source preview/apply service, workflow panel | Backend `test_services/test_workflow_sources.py`: `test_python_materialization_uses_workflow_local_factory_and_fresh_helpers`, `test_python_materialization_is_disabled_in_webapp_mode` | No browser Build from Python source preview/confirm/cancel journey found. It must use an owned local authoring fixture and prove later reopen uses the materialized graph. | S=G: exercise the named successful behavior and assert the exact row outcome; R=G: exercise the row's invalid-input or refused-action boundary and assert no unintended mutation; F=G: exercise cancellation, failure, or recovery and assert coherent retained state; P=G: reload or reopen durable state and assert the row's specified identity and content; B=G: exercise the applicable identity, concurrency, ownership, or permission boundary |

## Prioritized maintenance and added journeys

1. The main workflow journey is covered by `canvas-interactions.spec.ts`, “builds, runs, inspects, saves, and reopens a real workflow through the GUI”: GUI create/open, exact single-click documentation without mutation, double-click and drag creation, pointer connection, numeric parameter edit, real Run, exact GUI rows, explicit Save, workflow switch, and saved-graph reopen are asserted together. Retain its Chromium and Firefox evidence when recording the integrated revision.
2. Complete everyday editing through coherent small journeys: rename, select, reconnect, copy/paste, enable/disable, undo/redo, shortcuts, and persistent state.
3. Follow the plan's nested-workflow and execution-lifecycle waves, then complete settings/layout, datasets, packages/custom tools, editor and Python-authoring features.
4. Close each remaining row's applicable invalid-input, failure/recovery, persistence, and boundary scenarios; lower priority does not waive final coverage.
5. Repair weak fixtures and assertion gaps when they prevent trustworthy feature validation; retain build-only smoke labels for import-truthiness tests until behavioral replacements justify removal.

The revised catalog regression must assert visible documentation and exactly one persisted node from the appropriate gesture; observing an emitted event alone is insufficient.
Assign stable feature IDs and record scenario status, deployment/browser obligations, exact selectors, and evidence revisions as rows are completed.

Every new graph fixture must have a stated tool/input contract, deterministic identities and parameters, and an accepted-validation assertion before it is used as positive evidence.
Retain focused unit tests for state-space cases that would make browser coverage repetitive; browser assertions must exercise actual controls rather than invoking component handlers or installing final state for the behavior being claimed.
Unexpected behavior whose fix is unclear follows the [Sol → Astra → owner escalation protocol](platform-test-plan.md); do not loosen assertions, add compatibility branches, or silently bless an invalid workflow.
