---
orphan: true
---

# GUI editing and authoring coverage inventory

This partial inventory records inspected assertions, not a claim that every listed test passed in the current campaign.
The campaign paused during audit for a separate workflow source-update identity issue; the maintenance and missing-journey items below have not yet been implemented or validated.
It covers local GUI editing and authoring; workflow lifecycle, recursive interfaces, results, integrations, and execution have complementary campaign inventories.
HPC, Parsl, distributed engines, remote execution, and parallel execution behavior are excluded.
Browser automation remains an opt-in lane called after substantial changes, using the authoritative commands in [Testing](../testing.md).

In the tables, browser paths are relative to `frontend/tests/e2e`, frontend paths to `frontend/src`, and backend paths to `backend/tests`.
Quoted titles are exact test selectors usable with `scripts/test focus e2e <file> --grep '<title>'` or `scripts/test focus unit <file> -t '<title>'`.
Backend method names are searchable selectors usable with `scripts/test focus backend <file> -k '<name>'`.
An API-seeded browser fixture proves the browser behavior asserted after setup; it does not prove the equivalent setup action works through the GUI.

## Shell, settings, and restoration

| Behavior and authority | Implementation | Existing assertion evidence | Missing or bounded evidence |
| --- | --- | --- | --- |
| Default docks and menu, v1 §3.2 | `App.vue`, `components/layout/AppShell.vue` | Browser `app-shell.spec.ts`: `renders Dockview layout with menu bar and panels`; `View menu toggles panel visibility` | Dock resize, drag/reorder, close/reopen, and layout persistence need a browser journey. |
| Empty workspace never creates a special workflow, v1 §4.3 | `services/startupWorkflow.ts`, `App.vue` | Browser `workflow-creation.spec.ts`: `shows a non-persistent chooser when no workflow exists`; unit `services/__tests__/startupWorkflow.test.ts`: `returns null instead of creating a special startup workflow when none can load` | Assert no workflow POST and unchanged server list as well as empty UI. |
| Recovery preference and obsolete identities, v1 §4.3; v2 §12 | `services/startupWorkflow.ts`, recovery persistence coordinator | Unit startup tests: `prefers a fresh recovery graph over the last-opened workflow`, `clears a recovery entry that is older than the saved workflow or durable draft`, `clears recovery for an obsolete workflow name and opens the known last workflow` | Browser restoration should distinguish accepted backend draft from newer IndexedDB fallback and prove exact workflow identity, graph, and valid binding. |
| Recovered edges have real geometry, v1 §3.3, §4.3 | `components/canvas/CanvasView.vue` | Browser `graph-persistence.spec.ts`: `auto-saved graph restores nodes and edges after reload` asserts two nodes, one edge, finite SVG path | Existing fixture selects any tool input/output without proving compatibility, and manually injects IndexedDB. Replace with deterministic schema-compatible fixtures and assert accepted validation. |
| Preferences loading and desktop availability, v1 §3.12 | `components/panels/SettingsPanel.vue`, settings store | Browser `settings.spec.ts`: `opens via Edit > Preferences... and shows the settings tabs`; `workflow-creation.spec.ts`: `desktop shell shows Create Tool even when settings are unavailable` | Add settings load-error retry and close/reopen behavior. |
| Persisted page-size choice, v1 §3.12.4 | Display settings section, settings store | Browser `settings.spec.ts`: `Display exposes the persisted Node Data page-size preference`; backend `test_routers/test_settings.py`: `test_patch_node_data_page_size` | Browser currently reads 250 only; change selection, verify PATCH, reload, and inspect a newly opened table. |
| OMERO cards and secret handling, v1 §3.12.6; v2 §14.1 | OMERO settings section, settings router/store | Browser `settings.spec.ts`: `OMERO instance cards keep fields and actions visible without horizontal scrolling`; backend `test_routers/test_settings.py`: `test_patch_omero_password_persists_metadata_only`, `test_patch_omero_duplicate_names_return_422`, `test_patch_omero_keyring_failure_returns_error_without_persisting` | Responsive rendering is covered; Add/Save/Duplicate/Remove and failure feedback need browser coverage with a controlled keyring boundary. Actual OS credential-store integration remains manual. |
| Desktop/editor settings and forbidden webapp mutation, v1 §2.4.6, §3.12 | settings models/router, editor/image-viewer sections | Backend `test_routers/test_settings.py`: `test_patch_unsafe_webapp_features_rejected`, `test_webapp_fiji_configuration_is_rejected`; browser Fiji settings explanation test | Native selection and actual external application launch remain manual. See integration inventory for viewers. |

## Canvas editing and parameters

| Behavior and authority | Implementation | Existing assertion evidence | Missing or bounded evidence |
| --- | --- | --- | --- |
| Inspect/add/select node from real catalog, v1 §3.3.1, §3.4; v2 §15 | ToolsPanel, active canvas commands, CanvasView | Browser `canvas-interactions.spec.ts` asserts the real seeded `SeedNumbers` package/schema: single-click opens and closes exact documentation without mutation, double-click preserves the empty-canvas viewport and persists one valid interactive node, and drag persists one node at the requested position. Unit coverage distinguishes both row gestures, isolates secondary actions, routes creation only to the active root/nested canvas, and rejects missing or disposed ownership. | ISSUE-003 first repaired the unhandled creation event; the owner subsequently assigned inspection to single-click and creation to double-click. Additional node naming/collision cases belong to their dedicated rows. |
| Fit large/small graphs and encoded paths, v1 §3.3.4 | CanvasView | Browser `canvas-viewport.spec.ts`: `centers a small workflow with a working folder grid @critical`, corresponding `large` case; checks bounds, centering, grid references, Run Selected visibility | User zoom/pan, 5% minimum, fit shortcut, first-node viewport preservation, and resize behavior remain gaps. |
| Whole-DataFrame edges and dynamic columns, v1 §3.3.1–3; library §3.5–3.6 | pins, edge codec, resolved-output store | Browser `canvas-interactions.spec.ts`: `new dynamic tools connect cleanly and expose their resolved columns` verifies Generate has no parameter pins, whole-DataFrame edge, accepted graph, exact execution rows | Add positional auto-grow/compaction with multiple upstreams and incompatible cross-region rejection without graph mutation. |
| Column input labeling and disconnect, v1 §3.3.3 | InputPin | Unit `components/canvas/__tests__/InputPin.test.ts`: `renders the display name without changing the handle field name`, `reserves a published input instead of allowing an internal connection`, `disconnects the incoming edge when a connected handle is grabbed` | Browser reconnect/disconnect via actual pointer interaction and accepted graph required. |
| Mapped/collective column appearance, v1 §3.3.2 | edge components and tool metadata | Unit `components/canvas/__tests__/edges.test.ts` | Browser geometry/reactive metadata update evidence is missing; row-consumption presentation is in scope, parallel execution is not. |
| Parameters immediately feed accepted Run, v1 §2.4.3, §3.5.3 | NodePanel, canvas commands, persistence coordinator | Browser `critical-operation-races.spec.ts`: `immediate Node Panel parameter edit is the exact accepted Run input`, `delayed parameter persistence keeps node presentation stable and success quiet` | Run transport is intercepted in the immediate-edit case: this certifies snapshot ordering, not execution. Add control matrix for enum, bool, numeric, list parse errors, default/reset, and connected-field read-only behavior. |
| Node display-name rename preserves ID, v1 §3.5.1 | NodePanel | Node panel specialized tests provide partial evidence; broad `NodePanel.test.ts` only checks import truthiness | Browser rename valid/duplicate names; assert stable edge IDs and no accepted change on duplicate. |
| Select, multiselect, collapse, enable/disable, v1 §3.3.1, §3.5 | CanvasView, ToolNode, NodePanel | Existing browser journey checks single selection; unit node/context-menu and execution-lock tests cover component branches | Browser bulk actions, empty-click deselect, collapse persistence, and enabling/disabling need journeys. |
| Undo/redo per action and isolated baseline, v1 §4.6 | `composables/useUndoRedo.ts`, CanvasView | Browser `canvas-interactions.spec.ts`: `repeated undo returns moved nodes to the loaded workflow baseline`; unit `composables/__tests__/useUndoRedo.test.ts`: `undo/redo round-trip`, `reset installs an isolated baseline and discards all prior history`, `push after undo clears redo stack` | Browser redo, parameter edit granularity, mixed node/edge edits, and switch-workflow history isolation are missing. |
| Clipboard preserves internal structure and changes identities, v1 §3.3.1; v2 §6.3 | `utils/clipboard.ts`, CanvasView | Unit `utils/__tests__/clipboard.test.ts`: `copies and pastes a complete workflow node while changing only parent identities`, `rejects records without the current strict discriminator envelope` | No browser clipboard journey found; copy connected selection, paste, verify remapped internal edges and omitted external edges, reject malformed data without mutation. |
| Keyboard commands respect active context and text entry, v1 §6; v2 §15 | CanvasView, canvas command routing | Unit `composables/__tests__/canvasCommands.test.ts`: `routes Save to the active nested canvas command`, `routes a shared node id only to the explicitly active fixed canvas`, `rejects late calls through a disposed fixed-canvas resource` | Browser Delete/Backspace, copy/paste, select-all, save, validate, fit, Escape, and search focus need active-canvas and input-focus assertions. |
| Processing resources and output templates, v1 §3.5.4 | NodeResourcesTab, output-template controls | Unit `components/panels/__tests__/NodeResourcesTab.test.ts`: `shows declared, override, and effective resources and emits reset`, `emits the strict graph resource field names and capacity grammar`; `utils/__tests__/outputTemplates.test.ts` | Add browser CPU/memory field persistence/reset and template validation. Exclude concurrency allocation and parallel scheduler behavior. |

## Tool catalog, authoring, and editors

| Behavior and authority | Implementation | Existing assertion evidence | Missing or bounded evidence |
| --- | --- | --- | --- |
| Search and selected-tool reveal, v1 §3.4 | ToolsPanel | Browser `workflow-creation.spec.ts`: `search filters tool list to empty on nonsense query`; unit `components/panels/__tests__/ToolsPanel.test.ts`: `reveals a selected node tool through collapsed categories and search, then clears its highlight` | Add browser positive fuzzy query by tags/category and selected-row scrolling; nonsense query alone does not prove fuzzy matching. |
| Create/custom rename/delete and editor opening, v1 §3.4 | ToolsPanel, CreateToolDialog, custom-tool service | Browser `hot-reload.spec.ts`: `custom DataFrame tool display name refreshes after an editor save` creates via GUI; unit CreateToolDialog invalid-name and POST cases; ToolsPanel custom action tests | Browser ProcessingTool creation, duplicate-name rejection, rename usage propagation, delete confirmation/cancel and blocked deletion remain gaps. Editor open is intercepted in current browser creation test. |
| Package versions/source install and errors, v1 §2.3, §3.4 | ToolsPanel, package services | Unit ToolsPanel tests cover URL/ZIP install, busy state, failed installed version, confirmation, uninstall, webapp restrictions | No dedicated package-management browser journey found. Use deterministic local package archives for UI operations; real published-package installation remains external certification. |
| Editable source watcher and code-only change, v1 §2.7 | tool_hot_reload service, WebSocket, registry store | Browser `hot-reload.spec.ts`: both tests exercise filesystem→backend→WebSocket→canvas and the real watcher test now fails explicitly if Playwright does not provision its isolated mutable fixture; backend `test_services/test_tool_hot_reload.py`: `test_polling_recovers_missed_custom_tool_save`, `test_custom_tool_reload_failure_preserves_previous_metadata`, `test_atomic_custom_tool_save_reloads_from_move_destination` | Browser invalid edit→visible error→valid edit recovery is missing. |
| Embedded editor startup/progress and stable iframe, v1 §2.4.9 | CodeEditorPanel, editor service/workspace | Unit `components/panels/__tests__/CodeEditorPanel.test.ts`: `shows extension progress, elapsed time, and opens the Logger panel`, `keeps the existing iframe mounted while opening another file`, `ignores a load event from a previous iframe target`; backend `test_services/test_editor.py`: `test_cold_tool_open_returns_workspace_before_opener_activation`, `test_plain_status_does_not_generate_managed_workspace`, `test_editor_reuses_extensions_across_launches_and_repairs_missing_or_changed_content` | No complete browser editor journey found. Controlled status/opener fixtures can prove UI; real code-server installation, terminal continuity, package read-only workspace, detached/native windows need explicit external/manual acceptance. |
| Node-addressed source opening, v2 §14 | NodePanel, node-editor router | Frontend `api/__tests__/editor.test.ts`; backend `test_routers/test_node_editor.py` | Browser open bound source with a same-named catalog tool and nested-session source fork is missing; do not substitute catalog opening for node opening. |
| Trusted Python authoring, v2 §11 | workflow source preview/apply service, workflow panel | Backend `test_services/test_workflow_sources.py`: `test_python_materialization_uses_workflow_local_factory_and_fresh_helpers`, `test_python_materialization_is_disabled_in_webapp_mode` | No browser Build from Python source preview/confirm/cancel journey found. It must use an owned local authoring fixture and prove later reopen uses the materialized graph. |

## Prioritized maintenance and added journeys

1. Replace test-owned ambiguous graph wiring and permissive environmental skips first, then verify exact tests in Chromium and Firefox.
2. Keep the rendered catalog-row regression aligned with the interaction contract: single-click toggles documentation without mutation, while double-click emits exactly one add-tool event naming that row.
3. Treat the import-truthiness tests in `CanvasView.test.ts`, `AppShell.test.ts`, and `NodePanel.test.ts` as build smoke only; remove them when behavioral replacements and existing build/type checks subsume them.
4. Add a core editing browser journey with explicit compatible fixtures: create nodes, wire, edit, rename, copy/paste, undo/redo, save and reload, asserting exact accepted graph after meaningful transitions.
5. Add separate negative interaction tests for forbidden pin regions, malformed parameters/clipboard, duplicate names, and text-field keyboard isolation.
6. Add persisted display settings and package/custom-tool management journeys, then editor and Python-authoring journeys using controlled integration boundaries.

Every new graph fixture must have a stated tool/input contract, deterministic identities and parameters, and an accepted-validation assertion before it is used as positive evidence.
Retain focused unit tests for state-space cases that would make browser coverage repetitive; browser assertions must exercise actual controls rather than invoking component handlers or installing final state for the behavior being claimed.
Unexpected behavior whose fix is unclear follows the [Sol → Astra → owner escalation protocol](platform-test-plan.md); do not loosen assertions, add compatibility branches, or silently bless an invalid workflow.
