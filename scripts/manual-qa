#!/usr/bin/env bash

set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

export UV_CACHE_DIR="${UV_CACHE_DIR:-${TMPDIR:-/tmp}/bioimageflow-platform-uv-cache}"

cd "$repo_root/backend"
exec uv run --frozen python -m tests.manual_qa "$@"
