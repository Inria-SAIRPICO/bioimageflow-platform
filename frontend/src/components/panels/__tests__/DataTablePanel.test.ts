import { describe, expect, it } from 'vitest'
import DataTablePanel from '../DataTablePanel.vue'

describe('DataTablePanel recursive output projection', () => {
  it('builds with recursive workflow output resolution', () => {
    expect(DataTablePanel).toBeTruthy()
  })
})
